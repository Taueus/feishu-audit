# 运行日志排查报告

- **排查时间**：2026-09-17 15:15 ~ 15:30
- **项目**：feishu-audit（飞书文档审核机器人）
- **排查对象**：`bot_runtime.log`（289 KB）、`manage_runtime.log`（11 KB）、`audit_*.log`、进程/端口/配置状态
- **修复提交**：`de09959`

---

## 一、结论速览

| # | 问题 | 级别 | 日志证据 | 状态 |
|---|---|---|---|---|
| 1 | 工作台卡推送到**机器人已被移出的会话**，每次启动空转 60 秒 | 🔴 严重 | 280 条 230002 错误 | ✅ 已修复 |
| 2 | 审核日志**跨天不滚动**，今天的记录写进启动那天的文件 | 🟠 中 | 9/17 记录写入 `audit_20260915.log` | ✅ 已修复 |
| 3 | 进程存活检测**解码崩溃**，`_pid_alive` 不可靠 | 🟠 中 | 16 次 UnicodeDecodeError | ✅ 已修复 |
| 4 | 工作台推送线程**裸崩**，抛整页堆栈 | 🟠 中 | 3 次 Traceback | ✅ 已修复 |
| 5 | 缓存文档堆积 **502 个 / 约 260 MB** 删不掉 | 🟡 待处理 | 9 次 SAFE_DELETE_FAIL_CLOSED | ⏳ 待决策 |
| 6 | 飞书长连接断线重连 | 🟢 正常 | 13 次 ERROR，均自愈 | 无需处理 |
| 7 | 新表无访问权限（`1310213 No view permission`） | 🟢 已消解 | 9/13 的 3 次 Traceback | 已换表 |

**服务当前状态**：面板 `http://127.0.0.1:8788` 在线，bot 已连飞书长连接，规则 r1–r9 全部启用，当前表格 19 个工作表 / 18 个可用（响应 7.7 s）。**当前无重复实例**（1 面板 + 1 bot）。

---

## 二、逐项分析

### 🔴 问题 1：工作台卡推送到失效会话（最严重）

**现象**：`bot_runtime.log` 中 280 条日志反复出现：

```
[workbench] 发送重试中: SendError(target_revoked, hint='Bot/User can NOT be out of the chat.', raw_code=230002)
[Lark] channel.card_patch: patch failed code=230001 msg=... bot or user is not the message's sender
[workbench] 推送失败(60s 超时): ...
```

**根因**：`bot.py:42` 把工作台卡的推送目标**硬编码**了：

```python
WORKBENCH_CHAT_ID = "oc_e7f67309de0261d6d0a581b277c7d0d0"
```

该会话里机器人已被移出（230002），而 `send_workbench_sync()` 会**无条件重试到 60 秒**才放弃——即便错误明确标注了 `retryable=False`。后果：

- 每次 bot 启动白白空转 **60 秒**，刷约 40 条重复日志
- 常驻工作台卡功能**完全失效**（推不出去、也刷新不了）
- 9/13 起累计 280 条噪声，占全部 WARNING 的一半以上

**修复**（`bot.py` + `fsaudit/config.py` + `config.yaml`）：

1. 推送目标改为从配置读取：`config.yaml → workbench_chat_id`，值更新为当前有效单聊 `oc_d21768e01f260f670d67bc404f6f4572`；留空则跳过推送
2. **不可重试错误立即放弃**：命中 `retryable=False` / `230002` / `TARGET_REVOKED` / `230001` 时直接退出，不再空转 60 秒
3. 清空 `workbench_state.json` 里失效的旧卡记录（9/8 的 `om_x100b66d7f...`）

**验证结果**（重启后实测）：

```
[workbench] 已推送审核工作台卡片 om_x100b658d2ae7a0a4c3465066f82e891 → oc_d21768e01f260f670d67bc404f6f4572
[workbench] ✅ 已置顶工作台卡 om_x100b658d2ae7a0a4c3465066f82e891（固定于会话顶部）
[workbench] 已原位刷新常驻工作台卡 om_x100b658d2ae7a0a4c3465066f82e891（未重复推送）
```

**0 秒成功**，且二次启动**原位刷新**不重复堆卡——功能完全恢复。

### 🟠 问题 2：审核日志跨天不滚动

**现象**：今天（9/17）15:13 的审核记录，写进了 `audit_20260915.log`；而 `audit_20260917.log` 里却是 15:23 写入的另一批内容。同一张工作表的结果被拆到两个文件。

**根因**：`fsaudit/engine.py` 的 `setup_file_logging()` **在引擎初始化时就把文件名定死**：

```python
log_file = os.path.join(BASE_DIR, "audit_%s.log" % date.today().strftime("%Y%m%d"))
```

bot 于 9/15 启动 → 之后所有审核都写 `audit_20260915.log`。危害：面板「审核日志」页按**当天日期**取文件，长期运行的 bot 会让该页面显示"暂无日志"。

**修复**：改为自定义 `_DailyFileHandler`，每次 `emit` 前检查日期，跨天自动切换到当日文件（并关闭旧句柄）。

**验证**：单测模拟"进程内状态停留在旧日期"场景，跨天后第二条日志正确写入当天文件，旧文件不再增长。

### 🟠 问题 3：进程存活检测解码崩溃

**现象**：`manage_runtime.log` 反复出现（共 16 次）：

```
Exception in thread Thread-1 (_readerthread):
  File "subprocess.py", line 1615, in _readerthread
    buffer.append(fh.read())
UnicodeDecodeError: 'utf-8' codec can't decode byte 0xd0 in position 0
```

**根因**：`manage.py` 的 `_pid_alive()` 用 `subprocess.run([...tasklist...], text=True)` 读输出，中文 Windows 下 tasklist 输出含非 UTF-8 字节 → reader 线程裸崩。后果：**进程存活判断不可靠**，可能误判 bot 已死而重复拉起（历史日志里确实出现过 bot 被反复重启）。

**修复**：加 `errors="replace"`。

**验证**：新面板启动后**不再出现** UnicodeDecodeError。

### 🟠 问题 4：工作台推送线程裸崩

**现象**：3 次 Traceback（9/13 14:04、14:28、14:57）终止于：

```
File "bot.py", line 515, in send_workbench_sync
    card = picker_card(get_sheets(force=True), workbench=True)
...
fsaudit.feishu.FeishuError: code=1310213 msg=No view permission
```

`get_sheets()` 抛错 → daemon 线程直接死亡，把整页堆栈打进日志，且后续无补救。

**修复**：新增 `_workbench_thread()` 包一层异常保护，任何异常都只打一行日志、不再裸崩。

### 🟡 问题 5：缓存文档堆积（待决策）

- `cache/` 现有 **502 个文件**（496 个 `.docx` + 6 个 `.doc`），合计约 **260 MB**
- 日志里 9 次 `SAFE_DELETE_FAIL_CLOSED`，原因统一为 `windows-sandbox-recycle-bin-unavailable`
- 说明：审核流程跑完后会清理下载的原文，但当前运行环境**回收站不可用**，代码走 fail-closed（宁可不删），于是文件持续堆积

**这不是代码 bug，而是环境限制**。两个可选方向（需你决定）：

1. **加降级清理**：对 `cache/` 这类程序自建的临时文件，回收站不可用时允许直接删除（带白名单校验，只删 cache 目录下的文档）
2. **手动定期清理**：保留 fail-closed 策略，由人工定期清空 `cache/`

### 🟢 问题 6–7：无需处理

- **长连接断线 13 次**（9/8–9/17）：错误均为 `receive message loop exit` / DNS 短暂失败，每次都自动重连成功——本机走代理，属正常抖动
- **新表无权限**（9/13）：`code=1310213 No view permission`，涉及旧表 `XjS6sscu0hO98qtFkNqcV30YnKc`；现已切换为 `H1GRsesFlh7tgetaIBRcYJcrnbb` 并验证 18 个工作表全部可读

---

## 三、遗留事项

1. **缓存清理**（问题 5）待你选择方案——目前 260 MB 且会继续增长
2. `bot.pid` 文件删除同样被 fail-closed 拦截，但因其会被覆写，无实际影响
3. 本地领先 GitHub 远端 **18 个提交**未推送
4. 面板「重启机器人」按钮在 15:19 / 15:20 / 15:27 被触发过 3 次（前端只在点击时调用，非自动行为）

---

## 四、排查方法备忘（下次可复用）

| 目的 | 命令 / 位置 |
|---|---|
| 面板状态 | `GET /api/state`（**不是** `/api/status`） |
| 机器人启停 | `POST /api/bot` + `{"action":"start\|stop\|restart"}` |
| 日志读取 | `GET /api/log?src=runtime\|audit&n=行数` |
| 表格连通性自检 | `POST /api/verify`（返回各工作表 usable 状态） |
| 日志按类型统计 | `grep -o "\[ERROR\]\|\[WARNING\]\|Traceback" bot_runtime.log \| sort \| uniq -c` |
| 进程详情 | PowerShell `Get-CimInstance Win32_Process` 导出 CSV 再读（直接输出不稳定） |
| 杀进程 | PowerShell `Stop-Process -Id N -Force`（Git Bash 的 `taskkill //PID` 参数会被拒） |
