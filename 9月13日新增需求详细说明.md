# 飞书文档审核机器人 · 9月13日新增需求详细说明

> 本文汇总 2026-09-13 当天新增/完成的需求，共 **12 项**（2 条全新规则 + 6 项规则逻辑完善 + 3 项功能性能界面 + 1 项文档对齐）。
> 每项含：需求原话 → 背景问题 → 实现方案 → 验证结果 → 影响文件 → 提交号。

---

## 一、新增审核规则（2 条全新规则）

### 1. 规则六 · 广告法绝对化用语（v2.1）

**需求原话**
> 避免文章中出现绝对化用语违反广告法。比如最强，最顶级，世界第一，最高级这些词，你自己选择是新增还是在现有的规则下增加。

**决策**：**新增独立规则六**，而非并入规则二。理由——广告法合规词库与业务违禁词口径完全不同，且需要正则支持组合形态，独立规则可单独启停。

**业务口径**
《广告法》第九条禁止使用「国家级 / 最高级 / 最佳」等绝对化用语（俗称极限词）。文章中出现「最强 / 最顶级 / 最高级 / 世界第一 / 销量第一 / NO.1 / 顶级 / 唯一」等任一项 → **不通过**。字面级零容忍，不调 LLM。

**实现方案**
- **新建词库** `wordbooks/absolute_terms.yaml`：
  - `terms`：**53 个完整词**走子串精确匹配。只收完整词，从源头避免误伤——「最强」「最顶级」是完整词入库，「最后 / 最近 / 第一次」不在库中不会被命中；
  - `patterns`：**4 组正则**捕获组合形态——`(世界|全球|销量|行业…)第[一1]名?`（世界第一/全球第1/销量第一名）、`(?:no|top)[.．\s]*1`（NO.1/TOP1/top 1）、`(?:史上|全网|全球…)[^，。；]{0,6}最`（史上销量最好）、`最[牛燃炸酷潮香醇爽劲鲜纯]{1,2}`（最牛/最香）；
- `rules.py` 新增 `load_absolute_terms()` + `rule6_hits()`：
  - **命中折叠**：某命中是另一命中的完整子串时只报最长者（命中「最顶级」不再重复报「顶级」）；
  - 展示上限 10 项，超出显示「等共 N 处」；
  - 非法正则静默跳过（词库手误不炸审核）；
- `audit_text()` 新增 `absolute=(terms, patterns)` 可选参数，**向后兼容**，engine/audit 调用点无需改动；
- `config.py / manage.py / panel` 打通 r6 开关全链路（默认开，面板规则卡新增开关行）；
- 词库每次审核运行时重载，**改词库无需重启服务**。

**验证结果**
- 7 组极限词样本全捕获（含 NO.1 / TOP1 / 全球第1 / 史上销量最好整词折叠）；
- 3 组易误伤句（最近 / 第一天 / 最后 / 第一次）**零误报**；
- r6 开关有效、折叠正确。

**影响文件**：`wordbooks/absolute_terms.yaml`(新)、`fsaudit/rules.py`、`fsaudit/engine.py`、`audit.py`、`fsaudit/config.py`、`manage.py`、`panel/index.html`、`config.example.yaml`、`README.md`、`需求文档-详细版.md`(新增 5.6 节)

**提交**：`93261fc`

---

### 2. 规则七 · FAQ 问答结构（v2.2）

**需求原话**
> 文章中如果有 FAQ 的段落，需要检查 FAQ 是否形成问答形式并且是 Q 和 A 的问答结构而不是中文的问答，你可以在现有的规则下增加。

**业务口径**
文章含 FAQ 段落时，FAQ 必须**形成英文 Q/A 问答结构**（Q…A…），**不得使用中文「问/答」形式**。无 FAQ 段落的文章本规则不适用（直接通过）。

**实现方案**（`fsaudit/rules.py → rule7_faq_check`）
1. **FAQ 段落定位**：短标题行（**≤20 字符**）命中 `FAQ / Q&A / 常见问题 / 常见问答 / 问题解答 / 答疑` 视为 FAQ 段落起点，段落范围延伸至文末；正文中长句提及「常见问题」不触发判定；
2. **不通过情形**（按优先级）：
   - FAQ 段落内出现中文问答标记「问：/答：/问题：/回答：/提问：/解答：」（允许编号变体如「问题一：/回答2:」）→ 不通过（即使同时存在 Q/A）；
   - 段落内无任何 Q/A 标记 → 不通过（未形成问答结构）；
   - 有 Q 无 A / 有 A 无 Q → 不通过（问答结构不完整）；
3. **合法形态**：`Q1：… A1：…`、`Q: … A: …`、`Q2. … A2. …`、Q+A 直接接中文等英文变体，成对出现即通过。

**关键细节**
- 「答案」已从中文标记中**剔除**——避免叙述行文「有人会问：…答案：…」被误伤；
- 规则七在 `audit_text()` 内部处理（`rules_enabled['r7']`，默认开），**engine.py / audit.py 调用点零改动**。

**验证结果**：12 + 4 组正负样本全过；英文 Q/A 六种形态通过，中文问答及编号变体拦截。

**影响文件**：`fsaudit/rules.py`、`fsaudit/config.py`、`manage.py`、`panel/index.html`、`config.example.yaml`、`README.md`、`需求文档-详细版.md`(新增 5.7 节，速查表更新为七条规则)

**提交**：`3760333`

---

## 二、规则逻辑完善（6 项，均由反馈问题驱动）

### 3. 规则一 · 词库收敛（22 → 6 条）

**需求原话**
> 规则一词库删除 国产模型和海外模型 只审核痕迹话术。

**改动**：`ai_terms.yaml` 从 22 条收敛到 6 条，删除全部 16 个模型名（豆包、deepseek、kimi、chatgpt、claude、文心一言、通义千问、讯飞星火等），只保留痕迹话术：**大语言模型、以下由AI生成、以下内容由AI、作为一名AI、作为AI助手、AI生成**。

**说明**：词库运行时重载，改完下次审核即生效，无需重启 bot。selftest 规则一用例同步改为痕迹话术命中。**不影响**规则二里同名的「AI生成」（那条是独立配置）。

**影响文件**：`wordbooks/ai_terms.yaml`、`selftest.py`、`README.md`、`需求文档-详细版.md`(5.1)

**提交**：`ee2b8e6`

---

### 4. 规则二 · 新增开头署名/时间戳检测

**需求原话**
> 需要在规则二增加一个违禁词审核。就是开头出现作者：xx ；时间：xx年x月这样的表述也是不能通过的。

**问题背景**：稿件开头出现「作者：xx」「时间：xx年x月」这类平台水印式署名/时间戳行文，属于不可通过项。

**实现方案**（`rule2_header_check`）—— 命中需**三条同时满足**：
1. 位于文章**开头前 6 个非空行**内（标题占第 1 行，正文中间出现不算）；
2. 行首是署名/时间类标签 + 冒号 + 内容：**作者 / 撰稿人 / 撰稿 / 记者 / 编辑 / 发布时间 / 发布日期 / 时间 / 日期**，英文 **Author / Writer / Date** 也算；
3. 时间/日期类还要求冒号后 **20 字内有数字**。

**防误伤设计**（均验证过不误拦）：
- 「时间管理：五个方法」「时间的礼物：…」这类**标题**——标签后必须紧跟冒号，不命中；
- 「时间：是最好的证明」这类**散文句**——后面没有数字，不命中；
- 「本文作者认为…」**正文提及**——不在开头行首，不命中；
- 标签字符间允许空白（「作 者：」）、NFKC 归一化挡全角变形、允许 `（作者：xx）` 括号前缀。

**验证结果**：18 组正负样本全过；**326 篇真实文章全量扫描只命中 1 篇**，且是真实署名（「作者：鸿途 编辑:IT168鸿途 2026-06」）——零误报。

**影响文件**：`fsaudit/rules.py`、`selftest.py`、`README.md`、`需求文档-详细版.md`(5.2)

**提交**：`b42fcc1`

---

### 5. 规则一/二/六 · 防变形双层匹配

**需求原话**
> 为了避免这种情况的发生 需要怎么完善规则一和规则二的逻辑 你去想一个方案。

**问题本质**：关键词匹配能被各种隐形字符变形绕过（空格、全角、零宽字符、插标点）。

**方案：把匹配从「子串匹配」升级为「归一化 + 变形容错」双层检测**
- **第一层 · 归一化快路径**：NFKC 全角转半角（ＡＩ→AI）+ 大小写折叠 + 去掉所有空白（含换行/全角空格）和零宽字符后做子串匹配——挡住空格、换行、全角、零宽空格几类变形；
- **第二层 · 分隔符容错**：词内相邻字符之间允许出现 **0~2 个软分隔符**（`-`、`·`、`_`、`、`、`，`、`/` 等）也能命中——挡住「AI-生成」「AI·生成」这类插标点写法；
- **误报防线**：不允许句末标点（。！？）作为分隔、分隔符最多 2 个字符、中间隔了别的字不算——因此「……AI。下一句：生成……」跨句拼接、「该AI方案生成」隔字短语都不会误报；
- **规则三关键词保持严格匹配不动**，避免「科洛百」这类短 token 因容错正则误伤；
- 规则六的 `patterns` 同时在**原文**与 **NFKC 文本**上各跑一遍（「世界ＮＯ.１」也能命中）。

**验证结果**：18 组变形样本全过；**326 篇真实文章零新增误报**（新逻辑多抓的都是旧逻辑也能抓到的，新增的是对未来变形写法的防御）。

**影响文件**：`fsaudit/rules.py`、`需求文档-详细版.md`(5.1/5.2 补「词匹配防变形」口径)

**提交**：`5e6b9b1`

---

### 6. 规则二 · 空格漏判修复（真实案例驱动）

**需求原话**
> [9月8日][行264] …深算纪元实力分析.docx -> 通过，本条文章明显在结尾处出现了含AI生成，违反了规则二你是怎么判定他通过的。

**根因定位**：文章结尾水印原文是「**（注：部分内容可能由 AI 生成）**」——「AI」和「生成」之间**夹了一个空格**。违禁词「AI生成」是普通子串匹配，「AI 生成」≠「AI生成」，规则一、二都没命中，其余规则也过 → 误判「通过」。这是 CSDN 平台自动加的防检测水印。

**修复**：`rules.py` 新增 `_squash()`（去空格/制表符/全角空格、保留换行），`rule1_hits / rule2_hits / rule6_hits(terms)` 全部改压缩后匹配。该文修复后规则一 + 规则二**同时命中**「AI生成」。（后续在本日第 5 项中进一步升级为完整双层匹配。）

**连带发现**：缓存全量扫描发现**共 9 篇**同款水印文章（具身智能/世界模型/全屋定制/康师傅/新生儿奶粉等题材），此前均可能漏判。

**影响文件**：`fsaudit/rules.py`、`selftest.py`

**提交**：`c495bb3`

---

### 7. 规则三 · 缩写 token 匹配 + 自有产品白名单

**需求原话**
> 不通过 规则三：竞品「千日菊时光棒精华」首次出现早于我方关键词 / 不通过 规则三：我方关键词（科洛百KLB）未在文中出现 —— 这两个不通过的结果判定错误。千日菊精华是属于本品牌旗下的产品，文中我方关键词出现了 KLB 就符合审核表标准而不是一定要出现完整的科洛百KLB，你去完善一下规则三的逻辑。

**两处误判根因**
1. **「我方关键词未在文中出现」误判**：旧逻辑要求「科洛百KLB」整个关键词作为子串出现，文中只写了「KLB」就判未出现；
2. **「竞品千日菊时光棒精华」误判**：竞品来自 LLM 实体识别，旧过滤只做「品牌名与关键词互为子串」判断——「千日菊时光棒精华」与「科洛百KLB」无子串关系，被当成了竞品。

**修复方案**
- **关键词缩写匹配**（`rules.py` 新增 `find_keyword` / `keyword_tokens` / `_tok_related` / `keyword_in_text`）：整词优先，未命中时按 **token 回退**——「科洛百KLB」拆出「科洛百」「KLB」两个 token（中文连续段 ≥2 字 / 拉丁字母数字段 ≥2 位），文中任一出现即算我方关键词命中，**不区分大小写**；
- **自有产品白名单**（`brands.yaml` 新增 `my_products` 段）：登记「千日菊时光棒精华」，LLM 识别出的实体命中白名单后**不计入竞品**——规则三、规则四都不会再误判；
- **同步改动**：`engine.py`（bot 路径）与 `audit.py`（CLI 路径）的竞品过滤、规则四/五触发条件全部换用新匹配逻辑。

**验证结果**：新建 `regress_rule3.py` 13 项场景全过；selftest 9 项全过，无回归。

**影响文件**：`fsaudit/rules.py`、`fsaudit/engine.py`、`audit.py`、`wordbooks/brands.yaml`、`regress_rule3.py`(新)、`README.md`、`需求文档-详细版.md`(5.3)

**提交**：`2a809d9`

---

### 8. 规则三 · 核心品牌词提取（组织后缀剥离）

**需求原话**
> 1.[9月8日][行101] 杭州夜游…攻略.docx -> 不通过 规则三：竞品「杭州宋城」首次出现早于我方关键 
> 2.[9月8日][行121] 2026适度水解奶粉…盘点.docx -> 不通过 规则三：我方关键词（宜品集团）未在文中出现 —— 这两条判定是错误的，因为杭州宋城、宋城就是本项目宋城演艺只不过是换了一种形式，同理第二条中文章出现了宜品xx的产品，也是包含于本项目宜品集团的。

**两处误判根因**（同源）：审核表关键词是「**核心品牌 + 组织后缀**」的全称——「宋城演艺」「宜品集团」都是一整个中文词块，拆不出「宋城」「宜品」；文中的「杭州宋城」「宜品怡贝」与全称无子串关系：
- 行101：「杭州宋城」匹配不上「宋城演艺」→ 被当竞品，且出现位置更早 → 误判；
- 行121：「宜品怡贝」匹配不上「宜品集团」→ 误判「关键词未出现」。

**修复方案：核心品牌词提取**
- 给关键词匹配加第三层 token——**组织后缀剥离**；
- `rules.py` 新增 `_ORG_SUFFIXES`（**40 个**组织/行业后缀：集团、控股、公司、科技、文化、演艺、影业、文旅、医药、乳业、食品、教育……）+ `_strip_org_suffix` 迭代剥离；
- `keyword_tokens` 输出**核心品牌词**（剩余 ≥2 字）：宋城演艺 → 宋城、宜品集团 → 宜品，核心词参与关键词出现判定和竞品剔除；
- `find_keyword / is_mine / is_own_product` 全部自动受益。

**验证结果**
- 两篇真实文章复测修复：行101「杭州宋城」不再判竞品、行121「宜品怡贝」命中关键词 ✅；
- 真竞品不受影响：飞鹤（对宜品集团）、横店影视城（对宋城演艺）仍正确拦截 ✅；
- `regress_rule3.py` 扩到 **19 项全过**（新增 6 个核心品牌场景），selftest 全过。

**遗留提醒**：行121 重审后仍会不通过——该文 FAQ 段落确实用中文「问×5、答×5」，规则七报错是事实存在，非误判。

**影响文件**：`fsaudit/rules.py`、`regress_rule3.py`、`需求文档-详细版.md`(5.3)

**提交**：`7b2f70f`

---

## 三、功能 / 性能 / 界面需求（3 项 + 1 项文档）

### 9. 审核提速（性能）

**需求原话**
> 审核数据量比较大，优化审核速度。

**瓶颈定位**：每行文档**串行**处理（下载 1-3s + 3 次 LLM 调用各 10-40s）+ 回写逐格 2 次 API × N 行 + sleep(0.15)。

**优化方案（四管齐下）**
- **行级并发**（`engine.py`）：`ThreadPoolExecutor`，`config.yaml → concurrency`（1-16，默认 4，1 = 串行），结果按行号重排保证回写/卡片顺序稳定；
- **附件下载缓存**：附件按 `file_token` 命名落盘，命中即跳过下载；
- **批量回写**：飞书 `batch_update`（单请求 ≤500 range），整批失败自动回退逐格，去掉逐行 sleep(0.15)；
- **并发安全**：feishu token 刷新、三个 LLM 缓存读写全部加 `threading.Lock`；
- **面板配置**：LLM 卡新增「并发审核数」下拉（1-16），POST 校验（99 拒绝、6 接受）。

**验证结果**：9月5日表 10 篇 force 全量 dry-run 两次——**首跑 10.6s（含下载）、二跑 6.3s（全缓存命中）**，结果与串行完全一致（5过5不过）、行号有序。

**踩坑记录**：面板 8788 出现双进程监听（17748 旧代码 + 29588 新代码），请求命中旧进程导致 concurrency 字段"不生效"假象；`Get-NetTCPConnection` 静默失败，`netstat -ano | grep 8788` 才发现两个 LISTENING，杀干净重启后正常。

**提交**：`4ad4427`

---

### 10. 机器人配置前端化（功能）

**需求原话**：app 凭证与文件夹 token 之前只能改 `config.yaml`，搬进面板。

**实现方案**
- `manage.py`：`public_config` 增 app_id / app_secret_set / folder_token；`update_config` 增 0) app 块（app_id 校验 `cli_` 前缀、app_secret 覆盖/清除）和 0.5) folder 块（解析链接或裸 token、清除开关）；凭证变更 notes 提醒需重启生效；
- `panel/index.html`：规则卡前插入「机器人应用凭证」span-12 卡片（App ID / App Secret password+eye / 文件夹 token / 清除开关 + 凭证状态徽章）；renderConfig / collect / FIELD_IDS / countDirty / clearFields 全接入。

**验证结果**：/api/state 返回新字段；POST 三类用例全对（bad app_id 拒、坏 folder 链接拒、同值保存"无变化"）；node 语法 OK。

**提交**：`a60d052`

---

### 11. 面板模块按审核流程顺序重排（界面）

**改动**
- 卡片顺序：**机器人凭证① → 审核表格② → LLM③ → 规则+违禁词④ → 列映射(进阶) → 运行状态**；
- 凭证卡改**折叠式**：凭证完整自动收起、不完整自动展开；
- 违禁词输入框从列映射高级折叠区**挪进规则卡**（规则二正下方）；
- 各卡加 `.step` 徽章（1/2/3/4/进阶）。

**验证结果**：node 语法 OK、id 唯一、面板重启后新 HTML 生效、bot 在线。

**提交**：`1116d8e`

---

### 12. 需求文档 v2.0 + 配置模板对齐 v8（文档）

**改动**：`需求文档-详细版.md` 全面重写为 v2.0；`README.md` / `config.example.yaml` 对齐 v8 口径。

**提交**：`17a2a6c`（9/11 遗留，本日一并提交）

---

## 四、汇总清单

| # | 分类 | 需求 | 提交 |
|---|---|---|---|
| 1 | 新规则 | 规则六 · 广告法绝对化用语 | `93261fc` |
| 2 | 新规则 | 规则七 · FAQ 问答结构 | `3760333` |
| 3 | 规则完善 | 规则一 · 词库收敛（22→6 条） | `ee2b8e6` |
| 4 | 规则完善 | 规则二 · 开头署名/时间戳检测 | `b42fcc1` |
| 5 | 规则完善 | 规则一/二/六 · 防变形双层匹配 | `5e6b9b1` |
| 6 | 规则完善 | 规则二 · 空格漏判修复 | `c495bb3` |
| 7 | 规则完善 | 规则三 · 缩写 token + 自有产品白名单 | `2a809d9` |
| 8 | 规则完善 | 规则三 · 核心品牌词提取 | `7b2f70f` |
| 9 | 性能 | 审核提速（并发+缓存+批量回写） | `4ad4427` |
| 10 | 功能 | 机器人配置前端化 | `a60d052` |
| 11 | 界面 | 面板模块按流程重排 | `1116d8e` |
| 12 | 文档 | 需求文档 v2.0 + 模板对齐 | `17a2a6c` |

**规则全景（截至 9/13，共七条）**

| 规则 | 层级 | 回答的问题 | 用 LLM | 默认 |
|---|---|---|---|---|
| 规则一 | 词面 | 有没有暴露 AI 工具 | 否 | ✅ |
| 规则二 | 词面 | 有没有违禁表述 / 开头署名时间戳 | 否 | ✅ |
| 规则三 | 全文级 | 谁先出场（我方须先于竞品） | 否 | ✅ |
| 规则四 | 观点块级 | 谁是大主角 | 是 | ✅ |
| 规则五 | 内容级 | 文章质量与可被 AI 收录 | 是 + 启发式 | ✅ |
| 规则六 | 词面 | 广告法合规（绝对化用语） | 否 | ✅ |
| 规则七 | 格式级 | FAQ 结构规范（英文 Q/A） | 否 | ✅ |

---

## 五、遗留待办（截至 9/14）

1. **12 个提交未推送**到 GitHub（含 9/11 的 `17a2a6c`）：`17a2a6c / a60d052 / 1116d8e / 4ad4427 / 93261fc / 3760333 / 2a809d9 / c495bb3 / ee2b8e6 / 5e6b9b1 / b42fcc1 / 7b2f70f`
2. **9 篇带「AI 生成」水印的旧文章**待确认是否清空机器审核结果重审
3. **规则六/七端到端验证**曾被阻——表格 `XjS6ss…YnKc` 应用无查看权限（`code=1310213`），需在飞书把应用加为该表协作者
4. 行121 重审后仍会因规则七（中文问/答）不通过——判定无误，如需放宽口径需另行确认
