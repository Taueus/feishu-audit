# 飞书文档审核机器人 · 审核规则详细说明

> 本文是**面向业务与作者**的规则说明书：把系统当前生效的 **9 条审核规则**逐条展开，写清「判定什么、怎么判、什么情况不判、词库在哪、怎么改」。
> 面向工程的实现规格见 [《需求文档-详细版》](需求文档-详细版.md) 第 5 章；本文与之口径一致，冲突时以代码 `fsaudit/rules.py` 为准。

| 项目 | 内容 |
|---|---|
| 适用版本 | 规则一/二/三/六/七/八（确定性）+ 规则四/五/九（LLM），代码截至 2026-09-15 规则八/九新增 |
| 审核对象 | 表格「回链」列中的 **`.doc` / `.docx` 附件**（其他链接、空值一律跳过） |
| 我方关键词来源 | 表格「项目」列（一格可填多个，用 `,` `，` `、` `;` ` ` 空格分隔） |
| 结果回写列 | 「机器审核」= 通过 / 不通过；「不通过原因」= 各规则原因以 `；` 拼接 |

---

## 0. 总览

### 0.1 九条规则一览

| 编号 | 规则名 | 判定层级 | 类型 | 一句话口径 | 任一命中 |
|---|---|---|---|---|---|
| 规则一 | AI 痕迹词 | 字面 | 确定性 | 文中出现 AI 痕迹话术 → 不通过 | 不通过 |
| 规则二 | 违禁词 + 开头署名/时间戳 | 字面 | 确定性 | 出现违禁词，或开头有「作者：xx / 时间：xx年x月」→ 不通过 | 不通过 |
| 规则三 | 我方关键词先现位置 | 全文 | 确定性 | 我方关键词必须先于任何竞品出现；压根没出现 → 不通过 | 不通过 |
| 规则四 | 观点级主角性 | 观点块 | LLM + 代码判罚 | 观点不得被竞品占据；我方不得出现在避雷/负面块 | 不通过 |
| 规则五 | AI 人味 & AI 收录友好度 | 内容 | LLM + 启发式 | 有 AI 模板硬伤 / 通篇纯推销 / 无我方可抽取断言 → 不通过 | 不通过 |
| 规则六 | 广告法绝对化用语 | 字面 | 确定性 | 出现极限词（最强 / 世界第一 / NO.1 …）→ 不通过 | 不通过 |
| 规则七 | FAQ 问答结构 | 格式 | 确定性 | 有 FAQ 段落时，必须是英文 Q/A 问答；用中文「问：/答：」→ 不通过 | 不通过 |
| 规则八 | 竞品联系方式 | 字面+位置 | 确定性 | 手机/400/座机/邮箱/网址/微信/QQ 出现在竞品名 ±60 字内 → 不通过 | 不通过 |
| 规则九 | 品牌负面描述 | 语义 | 本地快速路径 + LLM | 我方或竞品被负面描述（贬损/差评/丑闻/避雷/劝退）→ 不通过 | 不通过 |

### 0.2 判定如何聚合

- **一票否决制**：9 条规则各自独立判罚，只要有任意 1 条命中 → 整篇「不通过」；全部通过 → 「通过」。
- **原因不合并、全量列出**：「不通过原因」列会把所有命中规则用 `；` 连起来（如 `规则三：竞品「飞鹤」首次出现早于我方关键词；规则七：FAQ 段落使用了中文「问/答」形式`），方便作者一次改到位。
- **原因列拼接顺序**（实现顺序）：
  1. 规则一 → 规则二（违禁词）→ 规则二（开头的署名/时间戳）→ 规则三 → 规则六 → 规则七 → 规则八 → 规则九（本地强负面词，`audit_text()` 内部顺序）；
  2. 之后追加**规则四**（LLM 判定）；
  3. **规则五硬性失败**时理由插到**最前**（让作者先看硬伤）；规则五未触发硬性否决时，其**收录优化建议**追加到末尾；
  4. 最后追加**规则九**的 LLM 语义判定结果（仅本地快速路径未命中时才调用）。
- **确定性规则先跑、LLM 规则后跑**：规则一/二/三/六/七/八及规则九的本地路径不依赖网络、毫秒级出结果；规则四/五/九的 LLM 部分要调 LLM，且有前置条件（见各节「触发前置条件」），不满足就**静默跳过（不判罚）**，绝不会因为模型不可用而误判不通过。

### 0.3 开关（可独立启停）

配置在 `config.yaml → rules_enabled`，也可在运维面板「规则卡」逐条开关（默认全开）：

```yaml
rules_enabled:
  r1: true   # 规则一 · AI 痕迹词（确定性）
  r2: true   # 规则二 · 违禁词 + 开头署名（确定性）
  r3: true   # 规则三 · 关键词先现位置（确定性）
  r4: true   # 规则四 · 观点级主角性（LLM，产生 API 费用）
  r5: false  # 规则五 · AI 人味 & 收录友好度（LLM，产生 API 费用）
  r6: false  # 规则六 · 广告法绝对化用语（确定性）
  r7: true   # 规则七 · FAQ 问答结构（确定性）
  r8: true   # 规则八 · 竞品联系方式（确定性）
  r9: true   # 规则九 · 品牌负面描述（本地 + LLM，LLM 部分产生 API 费用）
```

> 说明：`true=启用`，`false=该规则整条跳过`。上表为**当前部署实例**的取值；代码默认值里 r1–r9 全部为 `true`。关闭 r4/r5/r9 的 LLM 部分可省费用；规则九关掉 LLM 后仍保留本地强负面词判罚（零费用兜底）。
> 注意 `ai_terms.yaml` 等**词库文件**与**开关**是两件事：词库改完即时生效，开关控制整条规则是否参与判罚。

---

## 1. 规则一 · AI 痕迹词（确定性）

### 1.1 判定口径

文章正文中出现 `wordbooks/ai_terms.yaml` 中任意一个词 → **不通过**，原因写作 `规则一：检测到AI痕迹词「xxx」`（多个命中一并列出）。不区分大小写。

**当前词库（仅 6 条，全部为「AI 痕迹话术」）**：

```
大语言模型 / 以下由AI生成 / 以下内容由AI / 作为一名AI / 作为AI助手 / AI生成
```

> **词库收敛历史（2026-09-13）**：原词库含大量**模型名**（国产模型、海外模型），导致正常提及技术名词也被判不通过。按业务口径收敛为「只审痕迹话术、不拦模型名」，由 22 条删减为 6 条。

### 1.2 防变形双层匹配（重点）

**背景真实案例**：9 月 8 日行 264 的文档，结尾 CSDN 水印写作「（注：部分内容可能由 AI 生成）」——「AI」与「生成」之间**夹了一个空格**，早期子串匹配直接漏判，文章被错误放行。为此规则一（及规则二/规则六）统一升级为**双层检测**：

| 层级 | 做法 | 拦截的变形 |
|---|---|---|
| 第一层（快路径） | 文本与词都做 **NFKC 全角转半角 + 大小写折叠 + 去掉所有空白与零宽字符**，再做子串匹配 | 空格 `AI 生成`、换行、全角 `ＡＩ生成`、零宽空格/软连字符等隐形字符 |
| 第二层（容错正则） | 词的**相邻字符之间允许 0~2 个「软分隔符」**，编译成正则再搜原文 | `AI-生成`、`AI·生成`、`AI_生成`、`AI、生成`、`AI/生成` 等插标点写法 |

- **软分隔符集合**：空白、零宽字符、`· • ・ ﹒ - ＿ _ ~ ～ * # & / \ | ｜ 、 ， , ． . : ： ; ； ' ’ " ” 「 」 『 』 ( ) [ ] 【 】 〈 〉 《 》`。
- **安全护栏（防跨句误报）**：
  - 分隔符**最多 2 个**；
  - **不允许句末标点**（`。！？!?`）出现在词内——避免「……这个 AI。下一句讲：生成……」被误拼成「AI生成」。
- **规则三不走这套逻辑**（关键词匹配保持严格整词/token 匹配，避免短 token 造成误伤），此设计沿用至今。

### 1.3 示例对照

| 文本片段 | 判定 |
|---|---|
| `本文由AI生成` | ❌ 命中「AI生成」 |
| `（注：部分内容可能由 AI 生成）` | ❌ 命中（第一层去空格） |
| `本文由ＡＩ生成` | ❌ 命中（第一层全角归一） |
| `AI-生成的内容` | ❌ 命中（第二层容错正则） |
| `这个AI。生成的内容…` | ✅ 不命中（跨句，句末标点被禁） |
| `采用大语言模型辅助写作` | ❌ 命中「大语言模型」 |
| `我们使用 DeepSeek / GPT-4 生成文案` | ✅ 不命中（模型名已在 2026-09-13 从词库删除） |

### 1.4 维护方式

- 词库文件：`wordbooks/ai_terms.yaml`，在 `terms:` 下每行一个（`- 词`）。
- **改完无需重启**：每次审核运行时重新加载。
- 增词原则：只加**痕迹话术**（如「以下是AI生成的回答」这类露馅句式），不要加正常技术名词/模型名，避免误伤。

---

## 2. 规则二 · 违禁词 + 开头署名/时间戳（确定性）

规则二有**两个独立判定分支**，任一命中即不通过。

### 2.1 分支 A · 违禁词精确匹配

- 词表在 `config.yaml → forbidden_words`，**默认 `["AI生成", "免责声明"]`**，可在面板/配置中增删。
- 匹配同样走 **1.2 的双层防变形检测**（去空白、全角归一、插标点容错）——所以「AI 生成」（夹空格）不会像早期那样漏掉。
- 命中 → 原因 `规则二：出现违禁词「xxx」`。

### 2.2 分支 B · 开头署名 / 时间戳检测（2026-09-13 新增）

**业务口径**：稿件开头出现「**作者：张三**」「**时间：2026年9月**」「**日期：…**」这类署名/时间戳行文 → 不通过。这类「元信息头」是典型的内容农场/搬运痕迹，正式稿件不应带。

**判定规则（精确）**：

1. **只看开头**：仅检查正文**前 6 个非空行**；正文中间出现「作者」等字样**不算**。
2. **必须是行首标签**：标签须位于行首（允许前面有 `（` `【` `[` 等括号），后跟冒号。因此 `时间管理：三个技巧`、`作者观点：…` **不命中**。
3. **时间/日期类必须带数字**：`时间 / 日期 / 发布时间 / 发布日期 / time / date` 这些标签，**冒号后 20 字内必须出现数字**才判命中——防 `时间：是最好的证明` 这类散文句误伤。
4. **容错**：标签字符间允许空白（`作 者 ：` 也算）、NFKC 归一化（全角冒号 `：`、全角字母均可）。

**标签表**：

| 类别 | 标签 |
|---|---|
| 人员署名 | 作者、撰稿人、撰稿、记者、编辑 |
| 时间戳 | 发布时间、发布日期、时间、日期 |
| 英文 | author、writer、date |

**示例对照**：

| 开头内容 | 判定 |
|---|---|
| `作者：张三` | ❌ 命中 |
| `作 者：李四`（标签夹空格） | ❌ 命中 |
| `发布时间：2026-09-13` | ❌ 命中 |
| `时间：2026年9月` | ❌ 命中 |
| `日期：9月13日` | ❌ 命中 |
| `Author: John` | ❌ 命中 |
| `时间：是最好的证明`（冒号后 20 字无数字） | ✅ 不命中（散文句） |
| `时间管理：三个实用技巧`（非行首标签语义） | ✅ 不命中 |
| 正文中段出现「作者：张三」（第 10 行） | ✅ 不命中（只查前 6 个非空行） |

### 2.3 实测

全量 326 篇样本跑过：仅命中 1 篇真实署名文章，**零误报**。

---

## 3. 规则三 · 我方关键词先现位置（全文级，确定性）

### 3.1 判定口径

1. **我方关键词必须出现**：文中找不到关键词 → `规则三：我方关键词（xxx）未在文中出现`。
2. **必须早于任何竞品**：以「我方关键词**首次出现位置**」与「任一竞品**首次出现位置**」比较；存在出现更早的竞品 → `规则三：竞品「xxx」首次出现早于我方关键词`。
3. 「项目」列为空 → 直接不通过（`规则三：「项目」字段未填写关键词`）。

### 3.2 关键词三级匹配（2026-09-13 两次修复的成果）

关键词的「出现」判定分三级回退，**越往后越宽松**：

| 级别 | 匹配方式 | 例子 |
|---|---|---|
| ① 整词 | 关键词全文整词匹配（不区分大小写） | 关键词「科洛百KLB」→ 文中必须有「科洛百KLB」 |
| ② token 回退 | 关键词拆成 **token**（中文连续段 ≥2 字、拉丁字母数字段 ≥2 位），任一 token 出现即算 | 「科洛百KLB」→ 文中只写 **`KLB`** 或 **`科洛百`** 也算出现 |
| ③ 核心品牌词 | 中文 token **迭代剥离「组织/行业后缀」**后的剩余部分（≥2 字），出现即算 | 「宋城演艺」→ **`宋城`**；「宜品集团」→ **`宜品`** |

**组织/行业后缀表（40 个，迭代剥离）**：

```
有限公司 责任公司 股份公司 控股集团 有限 集团 控股 股份 公司
智能科技 信息技术 网络科技 科技 互联网 网络 通信 软件
文化 传媒 传播 演艺 影业 文旅 旅游
实业 生物 医药 制药 食品 乳业 饮品
电器 电子 家居 服饰 服装 美妆 日化
新能源 汽车 教育 咨询 餐饮 酒店 农业
```

> 剔除的**关键位置**是从**左侧**优先剥离：`宋城演艺` → 去掉「演艺」→ `宋城`（≥2 字保留）；若剥离后不足 2 字则丢弃该核心词。

### 3.3 竞品来源与过滤

竞品清单 = **LLM 识别出的品牌实体** ∪ **`wordbooks/brands.yaml → known_brands` 词库命中**，然后逐条剔除：

- **我方**：与任一关键词存在子串关系，或 token 级相关（`is_mine`）——如关键词「科洛百KLB」与实体「KLB时光棒精华」共享 token `KLB` → 判为我方，不算竞品；
- **自有产品白名单**（`is_own_product`）：命中 `wordbooks/brands.yaml → my_products` 的实体不计竞品。

**当前白名单**：

```yaml
my_products:
  - 千日菊时光棒精华     # 本品牌旗下产品，2026-09-13 加入
```

> 白名单兜底逻辑：实体名与白名单词互为子串，或 token 级相关，都算命中。规则三、规则四**共用**这份白名单。

### 3.4 真实误判修复案例

| 日期 | 案例 | 误判原因 | 修复 |
|---|---|---|---|
| 09-13 | 文中只写「**KLB**」，关键词是「科洛百KLB」，被判「关键词未出现」 | 整词匹配过严 | 增加 **token 回退**（② 级） |
| 09-13 | 自有产品「**千日菊时光棒精华**」被判为竞品 | 竞品清单无白名单 | 新增 **`my_products` 白名单** |
| 09-13 | 「**杭州宋城**」「宋城演艺」→ 判竞品早于我方；行 121「**宜品集团**」被判未出现 | 中文关键词是单 token，拆不出核心品牌 | 增加 **组织后缀剥离**（③ 级） |

> 回归验证：`regress_rule3.py` 共 19 项用例全过；真实竞品（飞鹤、横店等）仍正常拦截，无误放。

### 3.5 注意

- 规则三**不做防变形**（不适用 1.2 的双层匹配），保持严格——短 token 一旦容错会大量误伤。
- 规则三只管「**谁先出场**」；「谁是大主角」由规则四管。

---

## 4. 规则四 · 观点级主角性（LLM 抽取 + 代码判罚）

### 4.1 为什么要这样设计

稿子**允许并需要竞品对比**，但必须突出我方。早期设想「按全文竞品/我方提及总量比对」会**误伤**「对比 5 个竞品但通篇以我为主角」的合规稿，因此改为**按观点块逐块判定**。

架构上采用 **「LLM 只抽取事实 + 确定性代码判罚」**：LLM 负责把正文切成观点块、填客观属性；**是否违规由 `judge()` 按固化口径判**——口径稳定、可复核、可留痕、可缓存。

### 4.2 LLM 抽取的字段

| 字段 | 取值 | 含义 |
|---|---|---|
| `name` | 文本 | 块名或一句话摘要 |
| `kind` | `negative` / `reco` / `list` / `info` | 负面块 / 带推荐倾向的块 / 盘点横评榜单块 / 其他有立场的块 |
| `mine_present` | 布尔 | 块内是否出现我方品牌/产品（含子品牌、系列名） |
| `mine_lead` | 布尔 | 我方是否该块主角（①首个被点名/排推荐首位 ②正面展开不少于任何单一竞品 ③无竞品被「其中/尤其/首推」单独拎出压过我方） |
| `comps_count` | 整数 | 块内被实质介绍/推荐的竞品品牌数（去重） |
| `evidence` | 文本 | 原文证据摘录，供人工复核 |

拆分要求：同一节内并列的多个推荐条目（`（1）场景一…（2）场景二…`、`①…②…`、带小标题的各小节）**必须各自单独成块**，不得合并成一个块。

### 4.3 判据（v8，仅两条，命中任一即不通过）

| 编号 | 判据 | 触发条件 | 原因文案 |
|---|---|---|---|
| 1 | **观点被竞品占据** `occupied` | 某 `reco` 推荐块**整块没有我方**，却成片推荐 **≥2 款**竞品 | `规则四：观点级主角性——观点「XX」被竞品占据、我方非主角：…` |
| 2 | **避雷块现我方** `mine_in_negative` | `negative`（避雷/别买/不推荐/翻车/劝退/差评）类块中**出现我方**品牌或产品 | `规则四：观点级主角性——负面/避雷场景出现我方：…` |

**豁免与不判（重要）**：

- `list`（产品盘点 / 阵营横评 / 榜单归位）类块**一律不判**——客观罗列无「推荐 XX」立场，不构成观点压制；真正带推荐倾向的会归到 `reco`，不会漏判。
- `reco` 块内竞品**只有 1 款**不判（单款竞品的场景补充，属正常对比）。
- `info` 及其他类型不判（保守策略）。
- **历史变更**：原判据「观点内竞品先于我方并占优」（`competitor_leads`）已**合并到规则三**（全文级位置比对），不再消耗 LLM 调用。

### 4.4 触发前置条件与降级

规则四**只在同时满足以下全部条件时**才调用 LLM：

1. 规则一/二/三/六/七**全部通过**（`reasons` 为空——已判不通过的文章不再花 LLM 钱）；
2. 规则四开关 `rules_enabled["r4"] = true`；
3. LLM 已配置 API Key；
4. **确实识别到竞品**（`competitors` 非空）；
5. **我方关键词确实在文中出现**（否则谈不到「主角性」）。

**任一不满足 → 规则四静默跳过（不判罚）**。LLM 调用异常时同样跳过并记日志，不会因为接口故障误判。

### 4.5 缓存

- 结果按 `md5(正文)` 缓存在 `cache/viewpoint_cache.json`，附 `kw`（关键词列表）与 `v`（口径版本）校验；同一篇重复审核**零调用**。
- **口径版本 `CACHE_VERSION = "v8"`**：判据/豁免变更时递增该值，历史缓存自动失效，无需手动删。

---

## 5. 规则五 · AI 人味 & AI 收录友好度（LLM + 启发式）

### 5.1 两层目标

一条稿件要同时过两层关：

1. **人味关**：人看、AI 检测器看都看不出是 AI 写的；
2. **收录关**：文章应能被 AI 搜索引擎/大模型抓取并引用，且引用结果有利于我方。

规则五把这两层拆成 **「硬性一票否决」+「收录优化建议」** 两档：硬性判死不通过，建议只返回给作者改稿、不判死。

### 5.2 硬性一票否决（任一命中 → 不通过）

| 编号 | 判据 | 判定标准 |
|---|---|---|
| 1 | **AI 模板硬伤** `ai_tone_heavy` | 以下**三项至少满足两项**：①模板套话堆砌（首先/其次/再者/综上所述/总而言之/值得一提的是/需要注意的是/不难发现/由此可见 等大量出现）；②通篇无第一人称经验（我/我们/实测/试喝/亲自 等出现 ≤1 次）；③零具体数字事实（无度数/价格/年份/工艺/奖项/具体型号等可验证数据） |
| 2 | **通篇只有推销** `ad_only_no_info` | 整篇只有产品推销，缺乏可被 AI 引用的客观资料（产品参数/价格档位/场景实测/对比/科普/工艺介绍/适用场景等） |
| 3 | **无我方可抽取断言** `!mine_citable` | 全文找不到**至少一句**「可直接回答用户问题」的我方断言（须同时含：明确场景 + 我方品牌/产品 + 可被引用的结论句）——AI 回答用户问题时无句可抄 = 零收录 |

原因文案前缀 `规则五：…`，并会**插到不通过原因的最前面**（让作者先看到硬伤）。

### 5.3 收录优化建议（不判死，随结果返回）

建议由**本地确定性启发式**从正文统计得出（零边际成本、口径可复核），当前 9 条：

| # | 触发条件 | 建议文案要点 |
|---|---|---|
| 1 | 我方断言句未出现在正文前 30% | 我方结论未前置，AI 偏好抽取首段内容 |
| 2 | 模板连接词出现 ≥8 处 | 连接词过多、AI 味偏重，建议改场景化叙述 |
| 3 | 第一人称经验词 <2 | 补亲身场景提升人味与可信度 |
| 4 | 具体数据点（数字+度/元/年/等）<3 | 补数值支撑，AI 引用偏好带数字的句子 |
| 5 | 无问题式小标题（含 ? / 怎么 / 如何 / 哪个） | 问题式标题 AI 检索命中率约 3 倍 |
| 6 | 无 FAQ/问答块 | 前置 3–5 组问答可显著提升 AI 抽取率 |
| 7 | 无时间/作者 E-E-A-T 信号 | AI 引用偏好可核实来源 |
| 8 | 长文（>1500 字）段落数 <6 | 分段过粗影响 AI 切片抽取 |
| 9 | 已触发 AI 模板硬伤 | 先重写首段与过渡段，再补数据与第一人称 |

建议文案前缀 `规则五·收录优化建议——…`，最多展示 4 条（超出显示「等 N 条建议」）。

### 5.4 触发前置条件与降级

1. `rules_enabled["r5"] = true`；
2. 已配置 LLM API Key（否则静默跳过）；
3. **我方关键词确实在文中出现**（无关键词则无「我方断言」可言，直接跳过）。

LLM 调用失败时：**硬性判据跳过**（不判不通过），但仍用本地启发式给出建议。结果按 `md5(正文)` 缓存于 `cache/ai_quality_cache.json`（口径版本 `CACHE_VERSION = "v1"`）。

> 费用提示：规则五每篇文档调一次 LLM。当前部署实例中 `r5: false`（已关闭）以省费用。

---

## 6. 规则六 · 广告法绝对化用语（确定性）

### 6.1 判定口径

《广告法》第九条禁止「国家级 / 最高级 / 最佳」等绝对化用语（俗称「极限词」）。文章命中任一词 → **不通过**，原因 `规则六：出现广告法绝对化用语「xxx」`。字面级零容忍，不调 LLM。

### 6.2 词库结构（`wordbooks/absolute_terms.yaml`）

**`terms` —— 53 个完整词，走子串精确匹配**（仍走 1.2 的双层防变形检测）。只收**完整词**是刻意设计，从源头避免误伤：

| 类别 | 示例 |
|---|---|
| 「最」字类 | 最强、最佳、最好、最优、最顶级、最高级、最高端、最权威、最畅销、最低价、最受欢迎… |
| 前缀强化类 | 史上最、全网最、全国最、全球最、业内最 |
| 「第一」类 | 世界第一、全球第一、全国第一、行业第一、销量第一、销量冠军、排名第一、第一品牌、第一选择 |
| 级别类 | 国家级、世界级、国际级 |
| 极限形容类 | 顶级、顶尖、极致、极品、完美、无与伦比、绝无仅有、独一无二、遥遥领先、独家、唯一、首选、首创 |

**`patterns` —— 4 组正则**，捕获词表难以穷举的组合形态：

| 正则 | 命中示例 |
|---|---|
| `(?:世界\|全球\|全国\|全网\|全城\|行业\|业内\|市场\|销量\|口碑\|排名\|品类\|单品\|品牌)第[一1]名?` | 世界第一、全球第1、销量第一名 |
| `(?i)(?:no\|top)[.．\s]*1` | NO.1、TOP1、top 1 |
| `(?:史上\|全网\|全球\|全国\|业内)[^，。；！？\n]{0,6}最[^\s，。；！？\n]{0,2}` | 史上销量最好 |
| `最[牛燃炸酷潮香醇爽劲鲜纯]{1,2}` | 最牛、最香 |

### 6.3 命中处理

- **命中折叠**：某命中词是另一命中词的完整子串时，**只报最长者**（命中「最顶级」不再重复报「顶级」）。
- **展示上限 10 项**，超出显示「等共 N 处」。
- 正则匹配会**在原文与 NFKC 归一化文本上各跑一遍**（挡全角数字/字母变形，如 `ＮＯ.１`）。
- **非法正则静默跳过**（词库手误不会炸掉审核）。

### 6.4 示例对照

| 文本 | 判定 |
|---|---|
| `我们的产品是最强的` | ❌ 命中「最强」 |
| `全球第一的工艺` | ❌ 命中（正则） |
| `NO.1的选择` / `TOP 1` | ❌ 命中（正则） |
| `史上销量最好` | ❌ 命中（整词折叠，只报最长者） |
| `我们最后 / 最近 / 第一天 / 第一次…` | ✅ 不命中（非完整词、不在词表） |
| `顶级的享受` | ❌ 命中「顶级」 |

### 6.5 维护

- 词库文件 `wordbooks/absolute_terms.yaml`，每次审核运行时重载，**改完无需重启**。
- 增词原则：`terms` 只加**完整词**（避免「最」字裸词误伤「最近/最后」）；组合/变体形态加到 `patterns`。
- 面板开关 `rules_enabled["r6"]`（当前实例为 `false`）。

---

## 7. 规则七 · FAQ 问答结构（确定性）

### 7.1 判定口径

**仅在文章含有 FAQ 段落时生效**。要求 FAQ 段落必须是**英文 Q/A 问答结构**；出现中文「问：/答：」形式 → 不通过。

### 7.2 FAQ 段落如何识别

- **标题行**：某行**长度 ≤20 字**且命中 `FAQ / Q&A / 常见问题 / 常见问答 / 问题解答 / 答疑` → 该行起视为 FAQ 段落，判定区间 = 从该行到文末。
- **正文提及不算**：长句里出现「常见问题」四个字不会触发判定（只有短标题行才算）。
- 全文找不到 FAQ 段落 → **规则不适用，直接通过**。

### 7.3 三种不通过情形

| 情形 | 判定正则/条件 | 原因文案 |
|---|---|---|
| **用了中文问/答** | 中文标记 `问/提问/问题` 或 `答/回答/解答` + 可选编号（一二三…/0-9）+ `：`（须在行首或句末标点之后） | `规则七：FAQ 段落使用了中文「问/答」形式（问×N、答×M），须改为英文 Q/A 问答结构` |
| **未形成问答结构** | FAQ 段落内既无 Q 标记也无 A 标记 | `规则七：检测到 FAQ 段落，但未形成问答结构（未发现 Q/A 标记）` |
| **结构不完整** | 只有 Q 没有 A，或只有 A 没有 Q | `规则七：FAQ 段落有 Q 无 A / 有 A 无 Q，问答结构不完整` |

**英文 Q/A 标记的合法形态**（`Q`/`A` + 可选编号 + 分隔符 `：:．.、)）`，或 Q/A 后**直接接中文**）：

| 合法写法 | 说明 |
|---|---|
| `Q1：… A1：…` | 标准英文标记 + 编号 |
| `Q: … A: …` | 无编号 |
| `Q2. … A2. …` | 用点号分隔 |
| `Q 适合送礼吗 A 很适合` | Q/A 后直接接中文，也算标记 |

### 7.4 示例对照

| FAQ 段落写法 | 判定 |
|---|---|
| `Q1：这款酒适合送礼吗？` + `A1：非常适合，包装精美。` | ✅ 通过 |
| `Q：度数多少？` + `A：52 度。` | ✅ 通过 |
| `问：这款酒适合送礼吗？` + `答：非常适合。` | ❌ 中文问/答形式 |
| `问题一：度数多少` + `回答：52 度` | ❌ 中文问/答形式（含编号变体） |
| 只有 `Q1：…` 没有 A | ❌ 结构不完整 |
| 只有 `A1：…` 没有 Q | ❌ 结构不完整 |
| 有 FAQ 标题但下面全是散文，无任何 Q/A | ❌ 未形成问答结构 |
| 全文无 FAQ 段落 | ✅ 规则不适用 |

> **历史修正**：「答案」一词曾作为中文 A 标记导致误伤，已从中文标记中剔除（现仅认「答/回答/解答」）。

### 7.5 维护

- 实现位置：`fsaudit/rules.py → rule7_faq_check()`；面板开关 `rules_enabled["r7"]`（默认开）。
- 已知噪音：真实案例行 121 因规则七（中文「问/答」×5）不通过——经复核**判定正确，非误判**。

---

## 8. 规则八 · 竞品联系方式（确定性，2026-09-15 新增）

### 8.1 判定口径

**文章中不得出现竞品的联系方式。** 判定方式：先用正则识别文中的联系方式，再检查该联系方式与任一竞品名（含 token 缩写）的**字符距离**——距离 ≤ 60 字即认定该联系方式属于竞品 → 不通过。

| 联系方式类型 | 识别形态示例 |
|---|---|
| 手机号 | `13812345678`（1 开头 11 位，独立出现，不匹配长数字串片段） |
| 400 电话 | `400-123-4567` / `4001234567` |
| 座机 | `010-12345678` |
| 邮箱 | `service@brand.com` |
| 网址 | `http(s)://…` / `www.xxx.com` |
| 微信号 | `加微信 ABC12345` / `微信号：xx` / `vx/wx/wxid` 前缀 |
| QQ 号 | `QQ：123456` / `扣扣号123456` |

### 8.2 关键设计：竞品近邻窗口（防误伤）

- **我方自己的联系方式不受影响**——只有联系方式紧邻**竞品名**（±60 字）出现才判不通过。文末放我方 400 电话、我方官网完全合规。
- 距离判定取**最近的竞品**：若联系方式 60 字内既有我方又有竞品，报距离更近的那个，避免张冠李戴。
- 竞品名匹配沿用规则三的 token 逻辑（「科洛百KLB」的缩写 KLB 也算）。

### 8.3 示例对照

| 文中表述 | 判定 | 原因 |
|---|---|---|
| 「想深入了解**竞品X**，可拨打客服电话 **13812345678**」 | ❌ 不通过 | 手机号紧邻竞品名 |
| 「**竞品X**详情可**加微信ABC12345**获取」 | ❌ 不通过 | 微信号紧邻竞品名 |
| 「如需选购可咨询我方客服 **400-123-4567**，或在官网 **www.lanhai.com** 下单」 | ✅ 通过 | 窗口内没有竞品名，属我方联系方式 |
| 竞品名与某电话号码相距 100 字（中间隔了整段内容） | ✅ 通过 | 超出 ±60 字窗口，无法认定归属 |

### 8.4 维护

联系方式类型是**代码内置正则**（`fsaudit/rules.py → _CONTACT_PATTERNS`），新增形态（如「抖音号：xxx」）需改代码并重启 bot；窗口大小 `_CONTACT_WINDOW` 同理。竞品名单仍由 LLM 识别 + `brands.yaml` 词库提供。

---

## 9. 规则九 · 品牌负面描述（本地快速路径 + LLM 语义判定，2026-09-15 新增）

### 9.1 判定口径

**文章不能出现品牌（我方）和竞品的负面描述**，两个方向都拦：

- **我方被贬损** → 不通过（自砸招牌）；
- **竞品被贬损** → 不通过（涉嫌商业诋毁 / 不正当竞争风险）。

负面描述包括：贬损性评价（质量差/难用/失望/后悔/别买）、负面口碑（差评/投诉/维权/退货/骂声一片）、负面事件（丑闻/造假/被曝光/翻车）、避雷/踩雷/劝退类指向具体品牌的表述。

### 9.2 双层判定

1. **本地快速路径（确定性，零费用）**：`wordbooks/negative_terms.yaml` 里的强负面词（假货/山寨/翻车/塌房/智商税/割韭菜/劣质/避雷/劝退…）出现在品牌名（我方关键词或竞品）**前后 40 字内** → 直接判不通过，报距离最近的品牌。词库收词原则是「几乎只会用于贬损」的强负面词，防止误伤中性表述；**改词库即时生效，无需重启**。
2. **LLM 语义判定**：本地未命中时（且前序规则已全部通过、文中出现我方关键词或竞品），由 LLM 抽取隐性负面描述——如「竞品X用了三天就坏了」「网上骂声一片」这类不含强负面词的表述。LLM 只抽取事实（谁被负面描述+原文证据），**是否违规由代码固化判罚：命中任一条即不通过**。结果按正文哈希缓存（`cache/negativity_cache.json`），重复审不重复计费。

### 9.3 不算负面描述（防误伤，提示词中明确排除）

- **客观对比优势**：「我方在续航上优于竞品X」——对比可以，贬损不行；
- **未指向具体品牌的泛泛吐槽**：「市面上不少产品都虚标」；
- **无贬损色彩的客观陈述**：「竞品X成立于2010年」。

### 9.4 示例对照

| 文中表述 | 判定 | 层级 |
|---|---|---|
| 「相比之下**竞品X**近期**翻车**不断，口碑下滑」 | ❌ 不通过 | 本地强负面词 |
| 「网友吐槽**蓝海**定价偏高，直呼**智商税**」 | ❌ 不通过 | 本地强负面词（我方） |
| 「不少用户反馈**竞品X用了三天就坏了**，售后扯皮」 | ❌ 不通过 | LLM 语义判定 |
| 「**蓝海**续航表现稳健，实测**优于竞品X**」 | ✅ 通过 | 客观对比不算负面 |
| 「行业内个别小作坊曾出现翻车传闻」（与品牌相距 40 字外） | ✅ 通过 | 窗口外且未指向具体品牌 |

### 9.5 触发前置条件与降级

- LLM 部分：需配置 API Key、规则开关 r9 打开、且本地路径未命中（已判不通过就不再花 LLM 费用）、文中出现我方关键词或竞品；
- LLM 不可用时：**本地强负面词路径照常生效**（规则九不完全失效，只是隐性负面描述不判）。

---

## 10. 规则间关系与分工

| 规则 | 管的维度 | 关注问题 | 是否需要 LLM |
|---|---|---|---|
| 规则一 | 字面 | 有没有露馅的 AI 话术 | 否 |
| 规则二 | 字面 | 有没有违禁词 / 元信息头（署名、时间戳） | 否 |
| 规则三 | 全文 | **谁先出场**：我方必须先于竞品 | 否（仅竞品识别用 LLM，可词库兜底） |
| 规则四 | 观点块 | **谁是大主角**：观点不得被竞品占据、我方不得进负面块 | 是 |
| 规则五 | 内容 | **文章质量与可被 AI 收录**：人味 + AI 引用偏好 + 有我方断言 | 是（建议部分为本地启发式） |
| 规则六 | 字面 | **广告法合规**：绝对化用语零容忍 | 否 |
| 规则七 | 格式 | **FAQ 结构规范**：须为英文 Q/A 问答 | 否 |
| 规则八 | 字面+位置 | **竞品导流风险**：竞品联系方式不得出现 | 否 |
| 规则九 | 语义 | **商誉与合规**：我方与竞品都不得被负面描述 | 本地否 / 隐性负面用 LLM |

**为什么规则三与规则四不重复**：规则三在**全文级**比较「首次出现位置」（我方必须先出场，含「某观点块里竞品先于我方并占优」的情形）；规则四在**观点块级**判「这一块的立场是否被竞品占据 / 我方是否被放进了避雷名单」。两者互补，且规则四已**剔除**与规则三重叠的判据以省 LLM 费用。

**降级链路（保证不断审）**：

```
LLM 不可用 / 未配 Key
  → 竞品识别退化为本地词库 brands.yaml（规则三/八仍可判）
  → 规则四静默跳过
  → 规则五硬性跳过、仅出本地建议
  → 规则九退化为本地强负面词判罚（隐性负面不判）
  → 规则一/二/三/六/七/八 完全不受影响，照常判定
```

---

## 11. 配置与词库速查表

| 要改什么 | 改哪个文件/项 | 生效方式 | 归属规则 |
|---|---|---|---|
| AI 痕迹词 | `wordbooks/ai_terms.yaml → terms` | 改完即生效（运行时重载） | 规则一 |
| 违禁词 | `config.yaml → forbidden_words`（默认 AI生成、免责声明） | 下次审核 | 规则二 |
| 开头署名/时间戳标签 | `fsaudit/rules.py → _HEADER_META_LABELS` | 需改代码 | 规则二 |
| 竞品词库 | `wordbooks/brands.yaml → known_brands` | 改完即生效 | 规则三 |
| 自有产品白名单 | `wordbooks/brands.yaml → my_products` | 改完即生效 | 规则三/四 |
| 组织后缀表 | `fsaudit/rules.py → _ORG_SUFFIXES` | 需改代码 | 规则三 |
| 观点块判罚口径 | `fsaudit/viewpoint.py → judge()`（改后须递增 `CACHE_VERSION`） | 需改代码 | 规则四 |
| 规则五硬性判据 | `fsaudit/ai_quality.py → _judge_hard()`（版本 `v1`） | 需改代码 | 规则五 |
| 收录优化建议阈值 | `fsaudit/ai_quality.py → _suggestions()` | 需改代码 | 规则五 |
| 广告法极限词 | `wordbooks/absolute_terms.yaml → terms / patterns` | 改完即生效 | 规则六 |
| 品牌负面词（快速路径） | `wordbooks/negative_terms.yaml → terms` | 改完即生效 | 规则九 |
| 负面描述判定口径 | `fsaudit/negativity.py → judge()`（改后须递增 `CACHE_VERSION`） | 需改代码 | 规则九 |
| 联系方式识别正则/窗口 | `fsaudit/rules.py → _CONTACT_PATTERNS / _CONTACT_WINDOW` | 需改代码并重启 | 规则八 |
| 规则启停 | `config.yaml → rules_enabled`（或面板规则卡） | 下次审核 | 全部 |
| 我方关键词 | 飞书表格「项目」列（一格可多个） | 每次读表实时取 | 规则三/四/五 |
| 行级并发数 | `config.yaml → concurrency`（1–16） | 下次审核 | 性能 |

---

## 12. 端到端判定流程（一图看懂）

```
读表「回链」列 → 取 .doc/.docx 附件 → 下载（按 file_token 缓存）→ 解析出正文
        │
        ▼
  本地确定性规则（毫秒级，无需网络）
  ┌────────────────────────────────────────────────────────┐
  │ 规则一 AI痕迹词    命中 → 不通过                        │
  │ 规则二 违禁词 + 开头署名/时间戳  命中 → 不通过            │
  │ 规则三 我方关键词位置  未出现/晚于竞品 → 不通过           │
  │ 规则六 广告法绝对化用语  命中 → 不通过                    │
  │ 规则七 FAQ 结构  中文问答/结构残缺 → 不通过               │
  │ 规则八 竞品联系方式  紧邻竞品名 ±60 字 → 不通过           │
  │ 规则九(本地) 品牌负面强词  紧邻品牌名 ±40 字 → 不通过      │
  └────────────────────────────────────────────────────────┘
        │ 仍有 reasons？─── 有 ──► 直接定「不通过」（不再花 LLM 费用）
        ▼ 无（前六条全过）
  LLM 增强规则（需 API Key，结果按 md5 缓存）
  ┌────────────────────────────────────────────────────────┐
  │ 规则四 观点级主角性  观点被竞品占据 / 我方进负面块 → 不通过 │
  │ 规则五 AI人味&收录   AI模板硬伤 / 纯推销 / 无我方断言 → 不通过 │
  │                    （未触发硬性时仅返回「收录优化建议」）  │
  │ 规则九(LLM) 品牌负面描述  我方/竞品被隐性贬损 → 不通过     │
  └────────────────────────────────────────────────────────┘
        │
        ▼
  reasons 为空 → 「通过」；否则 → 「不通过」+ 原因以「；」拼接回写
```

---

## 附：规则修复与验证清单（近期）

| 日期 | 内容 | 提交 |
|---|---|---|
| 09-13 | 规则三：关键词 token 缩写匹配 + 自有产品白名单（修复科洛百KLB / 千日菊） | `2a809d9` |
| 09-13 | 规则一词库收敛：只审痕迹话术，删模型名（22→6 条） | `ee2b8e6` |
| 09-13 | 规则一/二/六防变形双层检测（修复「AI 生成」夹空格漏判） | `5e6b9b1`（含 `c495bb3` 空白不敏感） |
| 09-13 | 规则二新增「开头署名/时间戳」检测 | `b42fcc1` |
| 09-13 | 规则三增加核心品牌词（组织后缀剥离，修复杭州宋城 / 宜品怡贝） | `7b2f70f` |
| 09-13 | 规则六 · 广告法绝对化用语（53 词 + 4 正则） | `93261fc` |
| 09-13 | 规则七 · FAQ 问答结构（英文 Q/A 强制） | `3760333` |
| 09-13 | 规则四观点级审计 v8 + 规则五 AI 质量审计 + 五规则独立开关 | `a858892` |

**回归验证脚本**：

| 脚本 | 覆盖 |
|---|---|
| `selftest.py` | 解析 + 规则一/二/三（含空格变体、署名/时间戳用例，12 项） |
| `regress_rule3.py` | 规则三全量回归（19 项） |
| `regress_rule4b.py` / `regress_rule5.py` / `regress_viewpoint.py` | 规则四/五基线 |
