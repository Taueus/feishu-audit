# 飞书文档审核机器人 · 需求文档（详细版）

## 文档信息

| 项 | 内容 |
|---|---|
| 文档名称 | 飞书文档审核机器人 需求文档（详细版） |
| 版本 | v2.0 |
| 状态 | 现行有效（与代码 v8 口径一致） |
| 日期 | 2026-09-11 |
| 适用系统 | `D:\WorkBuddy\feishu-audit` |
| 对应代码基线 | 规则四 `CACHE_VERSION = "v8"`；规则五 `CACHE_VERSION = "v1"` |
| 读者 | 业务方（审核口径拍板人）、开发、运维 |

### 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-09-06 | 初版：规则一~三 + CLI + 飞书卡片机器人 |
| v1.5 | 2026-09-07 | 新增规则四（观点级主角性）、规则五（AI 人味 & 收录友好度）；五规则独立开关；运维面板「审核规则启停」 |
| **v2.0** | **2026-09-11** | 规则四收敛为 v8（删去「竞品先于我方并占优」判据，该场景交由规则三兜底）；补齐判据伪代码、触发前置条件、验收基线、已知限制 |

---

## 1. 背景与目标

### 1.1 业务背景

业务方在飞书共享电子表格中维护待发布的白酒营销稿件（每行一篇），「回链」列挂载 `.doc` / `.docx` 附件。稿件发布前需要人工逐篇审阅，判断是否存在以下问题：

- 含 AI 工具痕迹词（暴露"AI 写的"）；
- 含违禁表述（固定话术）；
- 竞品抢先出现、抢走观点主场，我方被弱化；
- 读起来像 AI 八股、且内容无法被 AI 搜索引擎/大模型抓取引用。

人工审核量大、口径不一致、无法沉淀。故建设本系统：**批量、可复现、口径固定**地审核并回写结果。

### 1.2 目标

| 编号 | 目标 | 衡量方式 |
|---|---|---|
| G1 | 审核结论可回写、可追溯 | 每行写回「机器审核」+「不通过原因」 |
| G2 | 审核口径稳定可复核 | 判罚由确定性代码执行，LLM 只做事实抽取 |
| G3 | 无需公网、本机可用 | 飞书长连接出站 + 面板仅监听 127.0.0.1 |
| G4 | 增量友好、成本可控 | 已审行跳过；LLM 结果按正文哈希缓存 |
| G5 | 规则可按业务节奏独立开关 | `rules_enabled{r1..r5}`，面板可切 |

### 1.3 范围

**做**：

- 从表格「回链」列取 `.doc` / `.docx` 附件，下载并解析全文；
- 执行规则一~五并给出通过/不通过 + 原因；
- 结果按需回写表格；
- 飞书单聊卡片式审核入口（常驻工作台卡 + 结果卡）；
- 本地运维面板（配置/状态/日志/启停）。

**不做**：

- 不审核发布链接（抖音/淘宝/公众号等），只审 `.doc/.docx` 附件；
- 不审核「备注」列等内部工作列；
- 不做正文改写/自动润色（只出建议，不改稿）；
- 不提供公网访问与多用户鉴权（本机单用户工具）。

### 1.4 术语表

| 术语 | 含义 |
|---|---|
| 我方 / mine | 「项目」列填写的品牌/产品关键词（一格可多个） |
| 竞品 / competitor | 文中出现的品牌实体中，不属于我方的部分 |
| 观点块 | 有立场的最小正文单元：某场景推荐、某价位推荐、避雷提醒、阵营盘点、榜单等 |
| kinds 分类 | `negative` 负面块 / `reco` 推荐块 / `list` 盘点横评块 / `info` 其他有立场块 |
| 确定性规则 | 不含 LLM、纯代码判定：规则一、二、三 |
| LLM 规则 | 需调用大模型：规则四、规则五 |

---

## 2. 用户与角色

| 角色 | 使用入口 | 关注点 |
|---|---|---|
| 审核业务方 | 飞书单聊卡片 | 结论是否合口径、原因是否可执行 |
| 稿件作者 | 飞书结果卡 | 不通过原因与收录优化建议 |
| 运维/开发 | 运维面板 `http://127.0.0.1:8788` | 配置、启停、日志、连通性 |

---

## 3. 总体流程

### 3.1 端到端主流程

```
① 数据定位   读工作表 → 表头校验（项目/回链/机器审核/不通过原因）
② 行筛选     回链列取附件 → 仅保留 .doc/.docx → 机器审核已有值则跳过（force 除外）
③ 文档获取   Drive 下载 → .doc 经 Word/WPS COM 转 .docx → 解析出 标题+正文
④ 品牌识别   LLM 抽取品牌实体 + 本地词库 → 剔除我方 → 竞品清单
⑤ 规则判定   规则一/二/三（确定性）→ 规则四（LLM 抽块 + 代码判罚）→ 规则五（LLM 硬性 + 本地建议）
⑥ 结论聚合   任一规则命中 → 不通过；原因按「规则N：具体命中内容」拼接
⑦ 结果输出   预览：仅展示；正式：确认后回写两列（限速 0.15s/格）
⑧ 留痕       审核日志 audit_YYYYMMDD.log + 卡片进度 + 面板日志
```

### 3.2 判定聚合与短路逻辑

```
reasons = []
reasons += 规则一命中项          # 若 r1 开
reasons += 规则二命中项          # 若 r2 开
reasons += 规则三未通过原因       # 若 r3 开

if 前三条全过 and 有竞品 and r4开 and LLM可用 and 我方关键词在文中:
    reasons += 规则四 issues

if r5开 and LLM可用 and 我方关键词在文中:
    if 规则五硬性命中:  reasons.insert(0, 规则五硬性原因)   # 前置，让作者先看硬伤
    elif reasons为空:   reasons += 规则五收录优化建议       # 仅在仍通过时给建议

passed = (len(reasons) == 0)
```

**关键约定**：

- 规则四**只在前三条全过时才调用**（节省 LLM 费用；前三条已否的稿子不必再看观点级）。
- 规则五**与前三条结果无关**，只要开关开、Key 有、我方关键词在文中就会调用；硬性原因插到 reasons 头部。
- 规则五的建议项**只在 reasons 仍为空时**输出，避免"不通过原因"里混入非致命建议。

---

## 4. 功能需求

| 编号 | 功能 | 说明 | 优先级 |
|---|---|---|---|
| FR-01 | 数据源与行筛选 | 按表头定位列；只审「回链」列 `.doc/.docx` 附件；非文档/空值计入"非文档跳过" | P0 |
| FR-02 | 文档获取与解析 | Drive 按 fileToken 下载；`.doc` 走 COM 转换（子进程 + 超时熔断）；解析出标题与正文 | P0 |
| FR-03 | 竞品识别 | LLM 抽取品牌实体（temperature=0）+ 本地词库兜底；剔除我方后作为竞品清单 | P0 |
| FR-04 | 规则一~三 | 本地确定性判定，即时生效，不依赖网络 | P0 |
| FR-05 | 规则四 | 观点块级判罚（occupied / mine_in_negative） | P0 |
| FR-06 | 规则五 | AI 人味 + AI 收录友好度；硬性一票否决 + 优化建议 | P0 |
| FR-07 | 规则启停 | 五条规则各自独立开关，配置持久化，面板可切 | P1 |
| FR-08 | 结果回写 | 写「机器审核」「不通过原因」两列；带限速；失败即中断并上报 | P0 |
| FR-09 | 增量补审 | 「机器审核」列已有值的行默认跳过；清空该格或 force 可重审 | P0 |
| FR-10 | 飞书卡片交互 | 常驻工作台卡（置顶）+ 选表 + 预览/正式 + 进度 + 结果卡 + 确认回写 | P0 |
| FR-11 | 运维面板 | 配置保存/显隐密钥/测试连接/启停进程/日志查看/规则启停 | P0 |
| FR-12 | 缓存 | 品牌识别、规则四、规则五结果按正文 md5 缓存；口径版本变更自动失效 | P1 |
| FR-13 | 日志 | 审核日志按日切分；面板分「运行日志 / 审核日志」两栏 | P1 |
| FR-14 | 进程托管与开机自启 | 面板托管 bot 子进程；开机自启脚本拉起全链路 | P2 |

### FR-01 数据源与行筛选

- 可审工作表判定：表头同时含「项目」与「回链」列（列名可配置）。
- 回链列读取：必须用 `valueRenderOption=Formula` 取值拿附件 `fileToken`（普通模式只返回 `[{'id':1,'type':'attachment'}]`）。
- 附件筛选：扩展名 ∈ `{.docx, .doc}`（大小写不敏感）。
- 跳过统计口径：`nondoc`（非文档或空）、`skipped`（机器审核已有值）。

### FR-02 文档获取与解析

- 下载路径：`cache/{sheet_id}_{文件名}`。
- `.doc` → `.docx`：调用 Word/WPS COM，**拆到独立子进程**（`fsaudit/doc_convert.py`）执行，支持超时熔断，避免 COM 卡死拖死主流程。
- 失败处理：下载或解析失败 → 该行判 **不通过**，原因 `文档下载或解析失败：{err}`，并计入 `error` 统计。

### FR-03 竞品识别

- LLM 只做**实体识别**，不做我方/竞品分类，也不做任何判定。
- 输出 JSON 字符串数组；`temperature=0`。
- 分类规则：`is_mine(brand) = any(brand in k or k in brand for k in 我方关键词)`；命中我方则不算竞品。
- 合并本地 `wordbooks/brands.yaml` 命中项，去重。
- LLM 失败/未配 Key：退化为纯词库模式（不中断审核）。

### FR-04 规则一 / 二 / 三

见 [第 5 章 审核规则详细规格](#5-审核规则详细规格)。

### FR-05 规则四 / 规则五

见 [第 5 章](#5-审核规则详细规格)。

### FR-07 规则启停

- 配置项：`config.yaml → rules_enabled: {r1..r5}`，缺省全 `true`；yaml 未给出的项保持默认，**显式 false 才关**。
- 行为：关闭的规则**完全不参与判罚**（不产生原因、不计入判定），规则四/五关闭时**不发起 LLM 调用**。
- 面板入口：「审核规则启停」卡片，5 个开关，保存即持久化。

### FR-08 结果回写

- 写两列：`机器审核` = `通过` / `不通过`；`不通过原因` = 原因串（通过时写空串）。
- 原因串格式：多条用 `；` 连接，条内格式 `规则N：具体命中内容`。
- 限速：每格 `sleep 0.15s`。
- 失败策略：遇到 `FeishuError` 立即 `break` 并返回 `{ok, failures}`，不静默跳过。
- 写入范围：必须使用 `P106:P106` 完整区间格式（单格简写 `P106` 会报 `90202 wrong range`）。

### FR-09 增量补审

- 默认：`机器审核` 列非空 → 跳过（含"有结果但原因为空"的半截数据）。
- 强制重审：`--force`（CLI）/ 面板 force / 清空单元格。

### FR-10 飞书卡片交互

```
发「审核」→ 弹「📋 审核工作台」卡（置顶常驻）
  → 勾选工作表（多选）→ 点「👀 仅预览」或「🚀 开始审核」
  → 系统发【独立新卡】实时刷新进度（当前文件/行、通过/不通过计数，2.5s 节流）
  → 完成后结果卡展示汇总 + 明细 + 不通过原因
  → 正式模式：点「✔ 确认回写飞书表格」才写入；「✖ 放弃不写」跳过
```

- 机器人每次启动自动推送/原位刷新工作台卡并置顶；重启不重复堆卡（`workbench_state.json` 记录 message_id）。

---

## 5. 审核规则详细规格

### 5.0 通用约定

- **判定对象**：`text = 标题 + "\n" + 正文`。
- **通过条件**：所有启用规则均未命中。
- **原因格式**：`规则N：…`，多条用 `；` 连接。
- **顺序**：一 → 二 → 三 → 四 → 五（规则五硬性原因被前置到首位）。
- 规则一/二/三为确定性规则，**零成本、即时、无网络依赖**。

---

### 5.1 规则一 · AI 痕迹词

| 项 | 规格 |
|---|---|
| 目标 | 稿件不得出现 AI 工具痕迹词 |
| 词库 | `wordbooks/ai_terms.yaml → terms`（当前 22 条） |
| 匹配 | 大小写不敏感的子串包含匹配 |
| 当前词库示例 | 豆包、deepseek、kimi、chatgpt、文心一言、通义千问、通义、讯飞星火、星火大模型、智谱清言、chatglm、claude … |
| 判定 | 命中任一词 → 不通过 |
| 原因 | `规则一：检测到AI痕迹词「X、Y」` |
| 伪代码 | `hits = [t for t in terms if t.lower() in text.lower()]` |

---

### 5.2 规则二 · 违禁词

| 项 | 规格 |
|---|---|
| 目标 | 稿件不得出现固定违禁表述 |
| 词表 | `config.yaml → forbidden_words`，默认 `["AI生成", "免责声明"]` |
| 匹配 | 大小写不敏感的子串包含匹配 |
| 判定 | 命中任一 → 不通过 |
| 原因 | `规则二：出现违禁词「X」` |
| 伪代码 | `hits = [w for w in forbidden_words if w.lower() in text.lower()]` |

> **口径说明**：本规则是"固定词表命中"，不做语义判断（早期"语义判断免责声明"的方案已废弃）。

> ⚠️ **已知限制**：子串匹配会误伤否定式表述。例如"本文不含 AI 生成内容"这类自证清白句同样会命中 `AI生成`。如需支持，需引入否定语境识别（当前未实现）。

---

### 5.3 规则三 · 关键词先现位置（全文级）

| 项 | 规格 |
|---|---|
| 目标 | 我方必须"先出场"——全文范围内我方关键词的首次出现位置必须早于任何竞品 |
| 输入 | `keywords` = 「项目」列拆分后的我方关键词；`competitors` = FR-03 识别结果 |
| 拆分规则 | 按 `,` `，` `、` `;` `；` `/` `|` `｜` 及空白切分 |
| 判定 1 | 「项目」列为空 → 不通过：`规则三：「项目」字段未填写关键词` |
| 判定 2 | 我方关键词**全部未出现** → 不通过：`规则三：我方关键词（X、Y）未在文中出现` |
| 判定 3 | 存在竞品首次出现位置 `0 <= pos < 我方最早位置` → 不通过：`规则三：竞品「Z」首次出现早于我方关键词` |
| 伪代码 | `my_first = min(find(k) for k in keywords if find(k) >= 0)`；`earlier = [b for b in competitors if 0 <= find(b) < my_first]` |

> **为什么不做全文提及量比对**：多竞品横评文天然竞品提及多，总量比对会误伤合规稿。规则三只管"谁先出场"，"谁是大主角"交给规则四。

---

### 5.4 规则四 · 观点级主角性（LLM 抽取 + 代码判罚）

#### 5.4.1 业务口径

稿件**允许且需要**竞品对比，但必须突出我方。审核**不按全文竞品/我方提及总量比对**，而是把正文切成「观点 / 小节 / 场景 / 价位档」逐块判定，看**单竞品品牌在特定观点/场景下是否占优**、观点是否被竞品占据、我方是否出现在负面名单里。

#### 5.4.2 架构：LLM 抽取事实，代码执行判罚

```
正文 ──► LLM「观点块解析器」 ──► blocks[]（客观属性，不做判定）
                                   │
                                   ▼
                        judge(blocks) 确定性判罚 ──► issues[]
```

LLM 对每个块**只填客观字段**，不发表审核意见：

| 字段 | 含义 |
|---|---|
| `name` | 块名或一句话摘要 |
| `kind` | `negative` / `reco` / `list` / `info` |
| `mine_present` | 块内是否出现我方品牌/产品（含子品牌/系列名） |
| `mine_lead` | 我方是否该块主角（①首个被点名/排推荐首位 ②正面展开不少于任何单一竞品 ③无竞品被"其中/尤其/首推"单独夸赞压过我方） |
| `comps_count` | 块内被实质介绍/推荐到的竞品品牌数（去重） |
| `evidence` | 一句原文证据摘录，供复核 |

拆分要求：同一节内并列的多个推荐条目**必须各自单独成块**（`（1）…（2）…`、`①②③`、带小标题的每小节），不得合并。

#### 5.4.3 现行判据（v8，仅两条）

| 判据 | 触发条件 | 原因文案 |
|---|---|---|
| `occupied` 观点被竞品占据 | `kind == reco` **且** `mine_present == False` **且** `comps_count >= 2` | `观点「X」被竞品占据、我方非主角：整块没有我方品牌，却成片推荐N款竞品，该观点成为竞品主场。{evidence}` |
| `mine_in_negative` 负面场景现我方 | `kind == negative` **且** `mine_present == True` | `负面/避雷场景出现我方：负面/避雷块中出现我方品牌或产品。{evidence}` |

**豁免与非判情形（代码固化）**：

| 情形 | 处理 | 理由 |
|---|---|---|
| `kind == list`（盘点/阵营横评/榜单归位） | **一律不判** | 客观罗列/归位，无"推荐XX"立场，不构成观点压制；我方是主角→天然合规，我方仅被提及→客观参考，整块无我方→客观归位 |
| `kind == reco` 且 `comps_count < 2` | 不判 | 单款竞品的场景补充，非"主场" |
| `kind == info` 或其他 | 不判 | 保守策略 |

#### 5.4.4 触发前置条件（必须同时满足）

1. 规则一/二/三**全部通过**（无 reasons）；
2. 竞品清单非空；
3. `rules_enabled["r4"] == True`；
4. LLM 已配置（`api_key` 非空）；
5. 我方关键词确实出现在正文中。

任一不满足 → 规则四跳过（不判不通过，不产生费用）。LLM 调用异常 → 记 warning 并本规则跳过。

#### 5.4.5 缓存

- 文件：`cache/viewpoint_cache.json`；键：`md5(正文)`；值含 `v`（口径版本）、`kw`（关键词快照）、`issues`。
- 命中条件：`v == CACHE_VERSION` **且** `kw` 与本次一致，否则重新调用。
- `CACHE_VERSION = "v8"`，判据/豁免变更时递增即可让历史缓存自动失效。

#### 5.4.6 口径演进（留档）

| 版本 | 判据 | 结果 |
|---|---|---|
| v1~v5 | LLM 直接判定（含多种豁免） | 判定震荡，豁免/边界反复失效，不可用 |
| v7 | 改为"LLM 抽事实 + 代码判罚"，三判据：①观点内竞品先于我方并占优 ②观点被竞品占据 ③避雷现我方 | 行 97 三条铁证（商务宴请/喜庆宴席/亲友小聚）稳定 |
| **v8（现行）** | **删除①**，仅保留 ②**观点被竞品占据** 与 ③**避雷现我方** | ①由规则三的全文级位置比对兜底 |

> **v8 的权衡（业务方已确认接受）**：删掉①后出现两处副作用——行 97 的"商务宴请"条不再单独标出（规则三是全文级位置比对，无法覆盖块级场景）；行 107 按现行口径翻为 FAIL。均按现行口径合规。

#### 5.4.7 输出

`issues[]` → `format_issues()` 拼成 `规则四：观点级主角性——{文案1}；{文案2}；等N处问题`（最多展示 3 条）。

---

### 5.5 规则五 · AI 人味 & AI 收录友好度

#### 5.5.1 业务口径

文章必须同时过两层关：**① 人看/AI 检测器都看不出是 AI 写的；② 文章应被 AI 搜索引擎/大模型抓取并引用，且引用结果有利于我方。**

#### 5.5.2 架构

- **LLM**：抽取三条硬性判据 + 一句我方可引用断言原文；
- **本地确定性启发式**：基于正文统计出优化建议（零边际成本、可复核）。

#### 5.5.3 硬性一票否决（任一命中即不通过）

| 判据 | 触发条件 | 原因文案 |
|---|---|---|
| `ai_tone_heavy` | 以下三项**至少满足两项**：a) 模板套话堆砌（首先/其次/综上所述/值得一提的是/需要注意的是/不难发现/由此可见…大量出现）；b) 通篇无第一人称经验（我/我们/实测/试喝/亲自 ≤1 次）；c) 零具体数字事实（无度数/价格/年份/工艺/奖项/具体型号） | `AI 模板硬伤：套话堆砌/无第一人称/零具体事实，人/AI 检测器一眼可识别` |
| `ad_only_no_info` | 通篇只有推销，缺乏可被 AI 引用的客观资料（产品参数/价格档位/场景实测/对比/科普/工艺介绍/适用场景等） | `通篇只有推销无可被引用的客观资料，AI 不会引用此类内容` |
| `!mine_citable` | 全文找不到至少一句"明确场景 + 我方品牌/产品 + 可被引用的结论句"的断言 | `全文没有『可直接回答用户问题』的我方断言，AI 回答时无句可抄，等于零收录` |

- 硬性原因写入 `reasons` **头部**（前缀 `规则五：`）。
- 逻辑依据：AI 检测原理是 **perplexity（困惑度）+ burstiness（句长方差）**——模板套话与均质长句同时拉低困惑度、拉平句长，故"套话 + 无第一人称 + 无数字"是可靠的特征组合。

#### 5.5.4 收录优化建议（不判死，随结果返回）

由本地启发式从正文统计得出，当前 9 条：

| # | 触发条件 | 建议文案 |
|---|---|---|
| 1 | 我方可引用断言未出现在前 30% | 我方结论未前置到前 30%，AI 偏好抽取首段内容 |
| 2 | 模板连接词命中 ≥ 8 处 | 模板连接词过多（N 处），AI 味偏重，建议替换为场景化叙述 |
| 3 | 第一人称经验词 < 2 处 | 第一人称经验细节不足，建议加入亲身场景提升人味与可信度 |
| 4 | 数据点（`\d+(°|度|元|块|年|ml|mL|%|％)`）< 3 处 | 具体数据点偏少，AI 引用偏好有数值支撑的句子 |
| 5 | 无问题式小标题（`？/?` 且无 `# 标题?`） | 缺问题式小标题，问题式标题 AI 检索命中率约 3 倍 |
| 6 | 无 FAQ/问答/常见问题块 | 无 FAQ 问答块，建议前置 3-5 组问答可显著提升 AI 抽取率 |
| 7 | E-E-A-T 信号 < 1 处（年份/月份/作者/来源/据…） | 缺时间/作者 E-E-A-T 信号，AI 引用偏好可核实的来源 |
| 8 | 段落数 < 6 且正文 > 1500 字 | 段落数过少，长文分段过粗影响 AI 切片抽取 |
| 9 | 已触发 `ai_tone_heavy` | 先重写首段与过渡段，再补具体数据与第一人称 |

- 建议**仅在 reasons 仍为空（即判定为通过）时**附加，前缀 `规则五·收录优化建议——`，最多展示 4 条。

#### 5.5.5 触发前置条件与降级

1. `rules_enabled["r5"] == True`；
2. LLM 已配置；
3. 我方关键词确实出现在正文中（无关键词则"可抽取断言"无从谈起）。

不满足 → 静默跳过。LLM 调用异常 → 硬性判据跳过，但**建议仍基于本地统计输出**（返回 `skipped=True` + `error`）。

#### 5.5.6 缓存

- 文件：`cache/ai_quality_cache.json`；键 `md5(正文)`；值含 `v`、`kw`、`result`。
- `CACHE_VERSION = "v1"`。

> ⚠️ **已知问题（未修）**：`ai_tone_heavy` 判据在营销文上误伤率高（回归样本 10 篇中 9 篇被判为"AI 八股"）。判据未区分「AI 模板味」与「营销文体（种草/导购/品牌话术）」，需重写判定标准，让该判据仅在真正 AI 痕迹时触发。

---

### 5.6 规则间关系速查

| 规则 | 层级 | 回答的问题 | 是否用 LLM | 默认开关 |
|---|---|---|---|---|
| 规则一 | 词面 | 有没有暴露 AI 工具 | 否 | r1 ✅ |
| 规则二 | 词面 | 有没有违禁表述 | 否 | r2 ✅ |
| 规则三 | 全文级 | **谁先出场**（我方须先于竞品） | 否 | r3 ✅ |
| 规则四 | 观点块级 | **谁是大主角**（观点是否被竞品占据、我方是否进负面名单） | 是 | r4 ✅ |
| 规则五 | 内容级 | **文章质量与可被 AI 收录**（人味 + 可引用） | 是 + 启发式 | r5 ✅ |

**边界归属**：某观点块内"竞品先于我方并占优"→ 由**规则三**（全文级位置比对）兜底，**不再**由规则四判定。

---

## 6. 数据结构与配置

### 6.1 `config.yaml` 字段

| 字段 | 类型 | 必填 | 默认 | 说明 |
|---|---|---|---|---|
| `app_id` | str | ✅ | — | 飞书应用凭证 |
| `app_secret` | str | ✅ | — | 飞书应用密钥 |
| `spreadsheet_token` | str | ✅ | — | 目标表格 token（支持贴完整链接自动解析） |
| `folder_token` | str | — | `""` | 附件所在云空间文件夹；留空则按文件名全盘搜索 |
| `llm.base_url` | str | — | `https://api.deepseek.com` | 任意 OpenAI 兼容地址 |
| `llm.api_key` | str | — | `""` | **留空 = 纯词库模式**，规则四/五自动跳过 |
| `llm.model` | str | — | `deepseek-chat` | 模型名 |
| `columns.keyword` | str | ✅ | `项目` | 我方关键词列 |
| `columns.link` | str | ✅ | `回链` | 附件列 |
| `columns.result` | str | ✅ | `机器审核` | 结果回写列 |
| `columns.reason` | str | ✅ | `不通过原因` | 原因回写列 |
| `forbidden_words` | list | — | `[AI生成, 免责声明]` | 规则二词表 |
| `rules_enabled.r1..r5` | bool | — | 全 `true` | 各规则独立开关 |

### 6.2 运行时文件（不入库）

| 路径 | 作用 |
|---|---|
| `cache/{sheet_id}_{文件名}` | 下载的待审文档 |
| `cache/brand_cache.json` | 品牌识别结果缓存（键：正文 md5） |
| `cache/viewpoint_cache.json` | 规则四判定缓存（含 `v` / `kw`） |
| `cache/ai_quality_cache.json` | 规则五判定缓存（含 `v` / `kw`） |
| `audit_YYYYMMDD.log` | 审核日志（按日切分） |
| `bot_runtime.log` / `manage_runtime.log` | 进程运行日志 |
| `bot.pid` | bot 进程号 |
| `workbench_state.json` | 工作台卡 message_id（防重复堆卡） |
| `config.yaml` | 实际配置（含密钥，**禁止入库**） |

### 6.3 回写列

| 列 | 写入内容 | 通过时 |
|---|---|---|
| `机器审核` | `通过` / `不通过` | `通过` |
| `不通过原因` | `规则N：…；规则M：…` | 空串 |

### 6.4 审核事件回调协议（`AuditEngine.run(on_event=cb)`）

| `type` | 载荷 |
|---|---|
| `log` | `msg` |
| `sheet_start` | `sheet` |
| `sheet_docs` | `sheet`, `total` |
| `row_start` | `sheet`, `row`, `file`, `index`, `total` |
| `row_done` | `sheet`, `row`, `file`, `kw`, `passed`, `reasons` |

---

## 7. 接口

### 7.1 飞书侧

| 类别 | 项 | 说明 |
|---|---|---|
| 应用能力 | 机器人 | — |
| 权限 | `im:message:send_as_bot` | 推送/更新卡片 |
| 权限 | `im:message.p2p_msg:readonly` | 接收单聊消息（发「审核」唤卡） |
| 权限 | `im:message.patch` | 原位更新卡片 |
| 权限 | `im:message.pin` | 工作台卡置顶 |
| 权限 | `sheets:spreadsheet` | 读表、回写 |
| 权限 | `drive:drive` | 下载回链附件 |
| 事件 | `im.message.receive_v1` | 长连接模式 |
| 事件 | `card.action.trigger` | 卡片按钮回调（走"回调配置"页签） |

> 关键前提：**必须发布版本**；表格必须把应用加为**可编辑**协作者。

### 7.2 面板 HTTP API

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/` | 面板页面 |
| GET | `/api/state` | 运行状态 + 配置摘要（不含密钥，只回 `api_key_set`） |
| GET | `/api/log?src=runtime\|audit&n=120` | 日志尾部 N 行 |
| POST | `/api/config` | 保存配置（字段留空 = 不改） |
| POST | `/api/verify` | 实测连通飞书并列工作表 |
| POST | `/api/bot` | `{action: start\|stop\|restart}` |

---

## 8. 非功能需求

| 类别 | 要求 |
|---|---|
| 部署 | 本机运行；面板仅监听 `127.0.0.1:8788`，无鉴权，禁止暴露公网 |
| 网络 | 全部出站：飞书长连接 WS + HTTPS；**无需公网入站** |
| 安全 | `config.yaml` 不入库；API 不回传密钥；`cache/` 属业务数据不入库 |
| 性能 | 单文档 LLM 调用上限 3 次（品牌识别 + 规则四 + 规则五）；二次审核走缓存为 0 次 |
| 稳定性 | LLM 超时 40s；任一 LLM 规则异常只跳过该规则，不中断整轮审核 |
| 可维护性 | 判据固化在代码 + `CACHE_VERSION` 版本化；口径变更只需递增版本即可失效旧缓存 |
| 兼容性 | Python 3.9+；Windows 需 `pywin32`（.doc 转换） |
| 可观测性 | 审核日志按日切分；面板运行日志/审核日志双栏；错误日志含 `code/msg` |

### 运行环境（本机）

- Python 解释器**必须使用 venv**：`C:\Users\22231\.workbuddy\binaries\python\envs\default\Scripts\python.exe`
  （全局 Python 的 `site-packages` 为空，直接跑会 `ModuleNotFoundError: No module named 'yaml'`）
- 启动：`manage.py` 启动面板 → 点「启动机器人」；或设 `FEISHU_AUDIT_AUTOSTART_BOT=1` 自动拉起 bot
- 开机自启：启动文件夹 `FeishuAudit.bat` → `start_manage.bat`（含 `PYTHONIOENCODING=utf-8`）

---

## 9. 验收标准

### 9.1 功能验收用例

| 编号 | 场景 | 预期 |
|---|---|---|
| AC-01 | 回链列是抖音/淘宝链接 | 跳过，计入"非文档跳过" |
| AC-02 | 机器审核列已有值 | 跳过；清空后可重审 |
| AC-03 | 正文含「豆包」 | 规则一不通过 |
| AC-04 | 正文含「免责声明」 | 规则二不通过 |
| AC-05 | 项目列填「今世缘、国缘」，正文先提「茅台」后提「今世缘」 | 规则三不通过 |
| AC-06 | 项目列为空 | 规则三不通过（未填写关键词） |
| AC-07 | 我方关键词全文未出现 | 规则三不通过 |
| AC-08 | 某推荐块整块无我方、成片推荐 ≥2 款竞品 | 规则四 `occupied` 不通过 |
| AC-09 | 负面/避雷块中出现我方品牌 | 规则四 `mine_in_negative` 不通过 |
| AC-10 | 竞品盘点/榜单块（list）无我方 | 规则四**不判**（豁免） |
| AC-11 | 推荐块含我方且我方为主角 | 规则四通过 |
| AC-12 | 关闭 r3 | 规则三不参与判罚、不产生原因 |
| AC-13 | 关闭 r4/r5 | 不发起对应 LLM 调用 |
| AC-14 | 未配 `api_key` | 规则四/五静默跳过，仅规则一/二/三生效 |
| AC-15 | 正式模式点「确认回写」 | 两列写入正确，读回一致 |
| AC-16 | 回写接口收到单格简写 `P106` | 自动补全为 `P106:P106`，写入成功 |
| AC-17 | 重启机器人 | 工作台卡原位刷新，不重复堆卡 |
| AC-18 | 同一文档二次审核 | 命中缓存，无新增 LLM 调用 |

### 9.2 规则四/五回归基线（10 行样本）

| 行 | 结论 | 命中规则 |
|---|---|---|
| 90 | 不通过 | 规则三 |
| 93 | 不通过 | 规则三 |
| 105 | 不通过 | 规则三 |
| 97 | 不通过 | 规则四（观点被竞品占据） |
| 107 | 不通过 | 规则四 |
| 106 / 108 / 109 / 110 / 111 | 通过 | — |

> 该基线随口径变更需同步更新；每次改判据**必须递增 `CACHE_VERSION` 并重跑回归**。

---

## 10. 已知问题与待办

| 编号 | 问题 | 影响 | 状态 |
|---|---|---|---|
| ISSUE-01 | 规则五 `ai_tone_heavy` 误伤营销文（10 篇误判 9 篇） | 规则五形同虚设 | ❌ 未修 |
| ISSUE-02 | 规则二子串匹配会命中「不含 AI 生成」等否定句 | 潜在误伤 | ⏳ 待确认是否需要 |
| ISSUE-03 | `manage.py` 子进程 reader thread `UnicodeDecodeError`（子进程输出含非 UTF-8 字节） | 日志被 traceback 污染，不影响主进程 | ❌ 未修（改 `decode(errors='replace')` 即可） |
| ISSUE-04 | 规则四 v8 副作用：行 97「商务宴请」条不再单独标出；行 107 翻 FAIL | 业务已知并接受 | ✅ 已确认接受 |
| ISSUE-05 | `wordbooks/brands.yaml` `known_brands` 为空 | 纯词库模式下无竞品可判 | ⏳ 待沉淀 |
| ISSUE-06 | 启动方式依赖 venv，误用全局 Python 会直接崩 | 运维踩坑 | ✅ 已写入 SOP（见 README / 运行环境） |

### 后续候选需求（未立项）

- 规则六 · E-E-A-T 信号评分（作者署名/资质/数据出处/真实案例 0-4 分）
- 规则七 · 结构化度（FAQ/小标题/表格/列表对 AI 抽取的友好度）
- 规则八 · 平台差异化（不同平台收录偏好差异）
- 规则四"结论卡"：把 `evidence` 一并回写到原因列，便于作者定位原文

---

## 11. 附录

### 11.1 规则判据速查表

```
规则一  AI 痕迹词      : 词库子串命中                        → 不通过
规则二  违禁词         : 词表子串命中                        → 不通过
规则三  先出场         : 我方全文首现晚于任一竞品 / 我方未出现  → 不通过
规则四  大主角         : reco 块无我方且竞品≥2（occupied）
                        negative 块含我方（mine_in_negative）  → 不通过
                        list 块一律豁免
规则五  质量与收录     : ai_tone_heavy / ad_only_no_info /
                        !mine_citable（任一）                  → 不通过
                        （另有 9 条优化建议不判死）
```

### 11.2 触发条件速查表

| 规则 | 需 LLM | 前置条件 |
|---|---|---|
| r1/r2/r3 | 否 | 开关开 |
| r4 | 是 | 开关开 + Key + **前三条全过** + 有竞品 + 我方关键词在文中 |
| r5 | 是 | 开关开 + Key + 我方关键词在文中 |

### 11.3 相关文件

| 文件 | 职责 |
|---|---|
| `fsaudit/rules.py` | 规则一/二/三 + 关键词拆分 + 列号工具 |
| `fsaudit/viewpoint.py` | 规则四（LLM 抽块 + `judge()` 判罚） |
| `fsaudit/ai_quality.py` | 规则五（LLM 硬性 + 本地建议启发式） |
| `fsaudit/llm.py` | 品牌实体识别 |
| `fsaudit/engine.py` | 审核编排：列工作表 → 逐行审核 → 回写 |
| `fsaudit/config.py` | 配置读写、默认值、token 解析 |
| `fsaudit/feishu.py` | 飞书 API 封装（token / Sheets / Drive） |
| `fsaudit/docparse.py`、`doc_convert.py` | 文本抽取、`.doc→.docx` 子进程转换 |
| `bot.py` / `manage.py` / `panel/index.html` | 卡片机器人 / 面板后端 / 面板前端 |
| `wordbooks/ai_terms.yaml`、`brands.yaml` | 词库 |
